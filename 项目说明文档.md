# 大学生心理健康教育与自测平台 — 项目说明文档

**版本**：v2.3  
**最后更新**：2026-05-07  
**状态**：✅ 核心功能已完成，持续迭代中

---

## 一、项目概述

面向大学生的心理健康教育与自测平台，提供「教育学习 → 专业自测 → 结果解读 → 个性化干预建议 → 持续追踪」的全流程心理健康服务。

| 维度 | 说明 |
|------|------|
| **前端** | UniApp（Vue.js）微信小程序 |
| **后端** | Spring Boot 2.7.18 + Maven |
| **数据库** | MySQL 5.7，utf8mb4 |
| **JDK** | 17（编译） |
| **服务端口** | `http://localhost:8080/api` |
| **API文档** | `http://localhost:8080/api/doc.html`（Knife4j） |

---

## 二、技术栈

### 后端

| 技术 | 版本 | 用途 |
|------|------|------|
| Spring Boot | 2.7.18 | 基础框架 |
| MyBatis Plus | 3.5.3.1 | ORM / 分页 |
| Spring Security | — | 认证加密（BCrypt） |
| JWT（jjwt） | 0.9.1 | Token 鉴权（7天有效期） |
| Knife4j | 3.0.3 | API 文档 |
| FastJSON2 | 2.0.40 | JSON 序列化 |
| Hutool | 5.8.22 | 通用工具 |
| MySQL Connector | 8.0.33 | 数据库驱动 |
| Redis（Lettuce） | — | 缓存（可选） |

### 前端

| 技术 | 说明 |
|------|------|
| UniApp + Vue.js | 跨端框架 |
| SCSS | 样式 |

---

## 三、项目结构

```
graduation_pro/
├── 项目说明文档.md                 ← 本文档（唯一真相源）
├── init_database.sql              # 数据库建表 + 基础数据
├── init_assessment_questions.sql  # 测评题目数据
├── init_recommendation.sql        # 推荐系统数据（用户/行为/画像）
├── add_more_resources.sql         # 补充资源
├── upgrade_assessment_report.sql  # ⚠️ 测评报告增强升级脚本
├── upgrade_resource_interaction.sql  # 教育资源互动升级脚本
├── upgrade_community_features.sql  # 社区功能升级脚本
│
├── backend/                       # Spring Boot 后端
│   ├── pom.xml
│   └── src/main/java/com/mentalhealth/
│       ├── MentalHealthApplication.java     # 启动类
│       ├── common/
│       │   ├── config/          # CorsConfig / MyBatisPlusConfig / SecurityConfig
│       │   ├── exception/       # BusinessException / GlobalExceptionHandler
│       │   ├── result/          # Result.java（统一返回体）
│       │   └── utils/           # JwtUtil.java
│       └── module/
│           ├── auth/            # 用户认证（controller/entity/mapper/service/dto/vo）
│           ├── resource/        # 教育资源
│           ├── assessment/      # 心理测评 ★
│           ├── emotion/         # 情绪日记
│           ├── community/       # 匿名社区
│           └── recommendation/  # 个性化推荐 ★★
│
└── frontend/                     # UniApp 前端
    ├── pages.json               # 路由 + TabBar 配置
    ├── App.vue / main.js
    ├── api/                     # 6个模块 API 封装
    ├── utils/request.js         # HTTP 请求拦截器（Bearer Token）
    └── pages/
        ├── index/               # 首页
        ├── login/ register/ forgot-password/
        ├── assessment/          # scale-list / test / result / history ★
        ├── emotion/             # diary-list / diary-edit / trend
        ├── community/           # post-list / post-detail / post-create
        ├── recommendation/      # index ★
        ├── resource/            # list / detail
        └── profile/             # index / edit / learning / privacy
```

---

## 四、快速开始

### 4.1 数据库初始化（按顺序）

```bash
# 1. 建表 + 基础数据
mysql -u root -p < init_database.sql

# 2. 测评题目
mysql -u root -p < init_assessment_questions.sql

# 3. 推荐系统数据
mysql -u root -p < init_recommendation.sql

# 4. 补充资源（可选）
mysql -u root -p < add_more_resources.sql

# 5. ⚠️ 测评报告升级（新增字段）
mysql -u root -p < upgrade_assessment_report.sql
```

### 4.2 配置后端

编辑 `backend/src/main/resources/application.yml`：

```yaml
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/mental_health?...
    username: root
    password: 你的密码
```

### 4.3 启动后端

```bash
cd backend
mvn clean compile -DskipTests
mvn spring-boot:run
```

### 4.4 启动前端

在微信开发者工具中导入 `frontend` 目录，编译运行。

### 4.5 测试账号

| 用户名 | 密码 | 角色 |
|--------|------|------|
| admin | admin123 | 管理员 |
| student001 ~ student008 | 123456 | 学生 |

---

## 五、功能模块详解

### 5.1 用户认证（auth）— 完成度 100%

| 功能 | 状态 |
|------|------|
| 用户名密码登录 + JWT Token | ✅ |
| 用户注册（学号/姓名/密码/学校/专业/年级） | ✅ |
| BCrypt 密码加密 | ✅ |
| 忘记密码页 | ✅ |

**API**：`POST /auth/login`、`POST /auth/register`  
**前端页**：`login.vue`、`register.vue`、`forgot-password.vue`  
**数据库**：`user`、`user_privacy`

---

### 5.2 教育资源（resource）— 完成度 95%

| 功能 | 状态 |
|------|------|
| 42+ 心理健康资源（文章/视频/音频） | ✅ |
| 6大分类导航 + 子分类 | ✅ |
| 类型筛选 + 关键词搜索 | ✅ |
| 资源详情 + 播放 | ✅ |
| 学习进度记录 + 浏览量统计 | ✅ |

**API**：`GET /resource/list`、`GET /resource/{id}`、`GET /resource/search`、`GET /resource/categories`、`POST /resource/{id}/study`  
**前端页**：`resource/list.vue`、`resource/detail.vue`  
**数据库**：`education_resource`（42条）、`resource_category`（12个）、`user_learning_record`、`resource_keywords`

---

### 5.3 心理测评（assessment）★ — 完成度 95%

| 功能 | 状态 |
|------|------|
| 3种专业量表（SDS / SCL-90 / UPI） | ✅ |
| 在线答题 + 进度保存 + 用时记录 | ✅ |
| **完整专业评分**（维度分析 + 因子分计算） | ✅ ★ |
| **权威解读生成**（基于临床心理学标准） | ✅ ★ |
| **分层级干预建议**（生活调适 → 专业干预） | ✅ ★ |
| **即时生成完整测评报告**（雷达图/维度/风险提示） | ✅ ★ |
| **测评档案**（历史记录 + 趋势图 + 时间线） | ✅ ★ |
| 提交后自动更新推荐画像 | ✅ ★ |

**评分能力**：

| 量表 | 题数 | 评分逻辑 |
|------|------|----------|
| **SDS** 抑郁自评 | 20题 | 正向/反向计分 → 粗分×1.25=标准分；四维度（情感/躯体/精神运动/心理） |
| **SCL-90** 症状自评 | 90题 | 10因子维度（躯体化/强迫/人际敏感/抑郁/焦虑/敌对/恐怖/偏执/精神病性/附加） |
| **UPI** 人格问卷 | 60题 | 关键题检测（自杀/情绪/焦虑/自信）+ 总分分类 |

**API**：

| 接口 | 方法 | 说明 |
|------|------|------|
| `/assessment/scales` | GET | 获取量表列表 |
| `/assessment/scales/{id}/questions` | GET | 获取题目 |
| `/assessment/submit` | POST | 提交答卷（返回完整报告） |
| `/assessment/history` | GET | 测评历史摘要 |
| `/assessment/report/{id}` | GET | ★ 单次完整报告 |
| `/assessment/reports` | GET | ★ 测评档案（含趋势） |

**前端页**：`scale-list.vue`、`test.vue`、`result.vue`（Tab式报告页）、`history.vue`（档案页）  
**数据库**：`assessment_scale`（3个）、`scale_question`（80题）、`user_assessment`（含 `dimension_scores` / `report_data` 字段）

---

### 5.4 情绪日记（emotion）— 完成度 90%

| 功能 | 状态 |
|------|------|
| 情绪评分（1-10）+ 情绪标签 | ✅ |
| 日记内容 + 天气/精力/睡眠 | ✅ |
| 日记列表 + 详情 | ✅ |
| 情绪趋势图 | ✅ |

**API**：`POST /emotion/diary`、`GET /emotion/diary/list`、`GET /emotion/diary/{id}`、`GET /emotion/statistics`  
**前端页**：`diary-edit.vue`、`diary-list.vue`、`trend.vue`  
**数据库**：`emotion_diary`、`emotion_statistics`

---

### 5.5 匿名社区（community）— 完成度 95%

| 功能 | 状态 |
|------|------|
| 匿名发帖 + 帖子列表（分页） | ✅ |
| 帖子详情 + 评论（楼中楼） | ✅ |
| 点赞/取消点赞 | ✅ |
| **内容分类筛选**（6大分类标签栏） | ✅ ★ |
| **帖子编辑**（仅作者可修改） | ✅ ★ |
| **帖子删除**（软删除，仅作者可操作） | ✅ ★ |
| **评论删除**（仅评论作者可操作） | ✅ ★ |

**API**：`POST /community/posts`、`GET /community/posts`、`GET /community/posts/{id}`、`PUT /community/posts/{id}`、`DELETE /community/posts/{id}`、`POST /community/posts/{id}/comments`、`GET /community/posts/{id}/comments`、`DELETE /community/comments/{id}`、`POST /community/posts/{id}/like`、`DELETE /community/posts/{id}/like`、`GET /community/categories`  
**前端页**：`post-list.vue`、`post-detail.vue`、`post-create.vue`（支持新建/编辑双模式）  
**数据库**：`community_post`、`community_comment`、`post_like`、`community_category`（6个分类）

---

### 5.6 个性化推荐（recommendation）★★ — 完成度 95%（核心创新点）

| 功能 | 状态 |
|------|------|
| 协同过滤（User-CF）：余弦相似度 | ✅ |
| 协同过滤（Item-CF）：资源相似度 | ✅ |
| 基于内容推荐：TF-IDF 标签匹配 | ✅ |
| **混合推荐**：加权融合（User-CF 35% + Item-CF 35% + Content 30%） | ✅ |
| 推荐理由可解释 + 匹配度可视化 | ✅ |
| 冷启动处理（热门资源补充） | ✅ |
| 推荐结果缓存（7天过期） | ✅ |
| 点击行为追踪 | ✅ |
| **测评结果联动推荐**（自动更新心理画像+兴趣标签） | ✅ ★ |

**推荐流程**：

```
用户提交测评 → 评分计算 → 更新 user_psychological_profile
                                    ↓
                          更新 user_interest_tags
                                    ↓
                  下次请求推荐时，Content-Based 匹配度提升
                  冷用户 → 热门资源兜底 → 混合推荐 TopN
```

**API**：`GET /recommendation/personalized`、`GET /recommendation/user-cf`、`GET /recommendation/item-cf`、`GET /recommendation/content-based`、`POST /recommendation/click`  
**前端页**：`recommendation/index.vue`（支持3种算法切换）  
**数据库**：`user_resource_rating`（29条）、`user_interest_tags`（15条）、`user_psychological_profile`、`resource_keywords`、`recommendation_result`

---

### 5.7 个人中心（profile）— 完成度 95%

| 功能 | 状态 |
|------|------|
| 用户信息展示（姓名/性别/年龄/学校/专业/年级） | ✅ |
| **个人信息编辑**（性别/年龄/学校/专业/年级/邮箱/手机） | ✅ ★ |
| **测评摘要**（总次数 + 最近结果 + 结果等级） | ✅ ★ |
| **测评历史列表**（最近5条，可点击查看报告） | ✅ ★ |
| **个人统计面板**（测评/日记/学习/帖子数量） | ✅ ★ |
| 学习记录查询 | ✅ |
| 隐私设置 | ✅ |

**API**：`GET /user/profile`、`GET /user/profile/full`、`PUT /user/profile`、`GET /user/learning`、`PUT /user/privacy`  
**前端页**：`profile/index.vue`（总览主页）、`profile/edit.vue`（资料编辑）、`profile/learning.vue`、`profile/privacy.vue`  
**数据库**：`user`（含 real_name/gender/age/university/major/grade）、`user_privacy`、`user_assessment`、`emotion_diary`、`user_learning_record`、`community_post`

---

## 六、数据库设计

### 6.1 表清单（20+ 张）

| 表名 | 模块 | 说明 |
|------|------|------|
| `user` | 认证 | 用户表 |
| `user_privacy` | 认证 | 隐私设置 |
| `education_resource` | 资源 | 教育资源（42条） |
| `resource_category` | 资源 | 分类表（12个） |
| `resource_keywords` | 推荐 | TF-IDF 关键词 |
| `user_learning_record` | 资源 | 学习记录 |
| `assessment_scale` | 测评 | 量表（3个） |
| `scale_question` | 测评 | 题目（80道） |
| `user_assessment` | 测评 | 测评记录（含 `report_data`/`dimension_scores`） |
| `emotion_diary` | 情绪 | 日记表 |
| `emotion_statistics` | 情绪 | 统计聚合 |
| `community_post` | 社区 | 帖子表 |
| `community_comment` | 社区 | 评论表 |
| `community_category` | 社区 | 分类表（6个） |
| `post_like` | 社区 | 点赞表 |
| `user_resource_rating` | 推荐 | 行为评分（29条） |
| `user_interest_tags` | 推荐 | 兴趣标签（15条） |
| `user_psychological_profile` | 推荐 | 心理特征画像 |
| `recommendation_result` | 推荐 | 推荐结果缓存 |
| `recommendation_cache` | 推荐 | 推荐缓存 |
| `user_behavior_log` | 推荐 | 行为日志 |
| `user_profile` | 推荐 | 用户画像 |
| `system_config` | 系统 | 配置项 |
| `admin_operation_log` | 系统 | 管理员日志 |

### 6.2 ER 关系概要

```
user ──1:N── user_assessment ──N:1── assessment_scale
  │                                      │
  │                                   1:N
  │                                      │
  │                                 scale_question
  │
  ├──1:N── emotion_diary
  ├──1:N── community_post ──1:N── community_comment
  ├──1:N── user_learning_record ──N:1── education_resource ──N:1── resource_category
  ├──1:1── user_psychological_profile
  ├──1:N── user_interest_tags
  └──1:N── user_resource_rating ──N:1── education_resource
```

---

## 七、API 接口清单

### 认证（2个）

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/auth/login` | 用户登录 |
| POST | `/auth/register` | 用户注册 |

### 资源（5个）

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/resource/list` | 资源列表（支持分类/类型/搜索/分页） |
| GET | `/resource/{id}` | 资源详情 |
| GET | `/resource/search` | 关键词搜索 |
| GET | `/resource/categories` | 分类列表 |
| POST | `/resource/{id}/study` | 记录学习进度 |

### 测评（6个）

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/assessment/scales` | 量表列表 |
| GET | `/assessment/scales/{id}/questions` | 题目列表 |
| POST | `/assessment/submit` | 提交答卷 |
| GET | `/assessment/history` | 测评历史 |
| GET | `/assessment/report/{id}` | ★ 单次完整报告 |
| GET | `/assessment/reports` | ★ 测评档案（含趋势） |

### 情绪（4个）

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/emotion/diary` | 创建日记 |
| GET | `/emotion/diary/list` | 日记列表 |
| GET | `/emotion/diary/{id}` | 日记详情 |
| GET | `/emotion/statistics` | 情绪统计 |

### 社区（11个）

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/community/categories` | 分类列表 |
| POST | `/community/posts` | 发布帖子 |
| GET | `/community/posts` | 帖子列表（分页+分类筛选） |
| GET | `/community/posts/{id}` | 帖子详情 |
| PUT | `/community/posts/{id}` | 编辑帖子 |
| DELETE | `/community/posts/{id}` | 删除帖子 |
| POST | `/community/posts/{id}/comments` | 发表评论 |
| GET | `/community/posts/{id}/comments` | 评论列表 |
| DELETE | `/community/comments/{id}` | 删除评论 |
| POST | `/community/posts/{id}/like` | 点赞 |
| DELETE | `/community/posts/{id}/like` | 取消点赞 |

### 推荐（7个）

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/recommendation/personalized` | 四路混合推荐（默认） |
| GET | `/recommendation/user-cf` | 协同过滤（带时间衰减） |
| GET | `/recommendation/item-cf` | 物品协同过滤（带时间衰减） |
| GET | `/recommendation/content-based` | 内容推荐 |
| GET | `/recommendation/profile-based` | ★ 心理画像推荐（冷启动） |
| POST | `/recommendation/click` | 记录点击 |
| POST | `/recommendation/refresh-tags` | ★ 刷新兴趣标签 |

### 用户（5个）

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/user/profile` | 个人信息 |
| GET | `/user/profile/full` | ★ 个人主页完整信息（含测评摘要/历史/统计） |
| PUT | `/user/profile` | 更新个人信息（支持性别/年龄/学校/专业等） |
| GET | `/user/learning` | 学习记录 |
| PUT | `/user/privacy` | 隐私设置 |

---

## 八、SQL 脚本执行顺序

| 序号 | 文件 | 说明 |
|------|------|------|
| 1 | `init_database.sql` | 建表 + 基础数据（必须） |
| 2 | `init_assessment_questions.sql` | 测评题目（必须） |
| 3 | `init_recommendation.sql` | 推荐系统数据（必须） |
| 4 | `add_more_resources.sql` | 补充资源（可选） |
| 5 | `upgrade_assessment_report.sql` | ⚠️ 测评报告升级（必须） |
| 6 | `upgrade_resource_interaction.sql` | 教育资源互动升级（必须） |
| 7 | `upgrade_community_features.sql` | 社区功能升级（必须） |

---

## 九、变更记录

| 日期 | 版本 | 变更内容 |
|------|------|----------|
| 2026-05-07 | v2.3 | **推荐系统全面升级**：三路→四路混合推荐（+心理画像推荐20%权重）；时间衰减融入CF计算；Min-Max归一化替代Max-Only；兴趣标签动态更新（基于30天行为日志）；多样性过滤；移除冗余表（user_profile/recommendation_cache）；行为日志表激活；推荐API 5→7个；重写个性化推荐文档v2.0 |
| 2026-05-07 | v2.2 | **个人中心全面升级**：个人信息编辑（支持性别/年龄/学校/专业等字段）；新增测评摘要+历史列表展示；个人统计面板（测评/日记/学习/帖子数量）；前端新增 edit.vue 编辑页；用户 API 3→5个 |
| 2026-05-07 | v2.1 | **匿名社区功能完善**：新增6大内容分类 + 分类筛选标签栏；实现帖子编辑/删除（仅作者）+ 评论删除；修复 unlikePost API 调用；社区 API 6→11个 |
| 2026-05-07 | v2.0 | **测评报告与档案**：SDS/SCL-90/UPI 维度评分、权威解读、分层建议、雷达图、历史档案页、推荐画像联动；新增 2 个 API + 2 个前端页 |
| 2026-04-06 | v1.0 | 初始版本：6 大模块基础功能完成（认证/资源/测评/情绪/社区/推荐） |

---

> **维护说明**：本文档是项目的唯一真相源。每完成一个功能模块的增删改，请同步更新对应的章节和变更记录表。
