# 个性化推荐系统实现流程详解

**版本**：v2.1  
**最后更新**：2026-05-07  
**适用场景**：答辩讲解、技术文档、学习参考

---

## 一、推荐系统概述

### 1.1 为什么需要四种算法？

推荐系统有一个经典问题：**单一算法无法覆盖所有场景**。比如：

- 新用户没有行为数据 → 协同过滤失效 → 需要心理画像推荐
- 用户兴趣固定 → 协同过滤只会推荐相似内容 → 需要内容推荐打破"信息茧房"
- 心理画像太粗糙（三个 level） → 仅靠画像区分度不够 → 需要协同过滤细化

四种算法各司其职，加权融合后形成 **互补推荐体系**：

| 算法 | 权重 | 核心问题 | 输入数据 | 依赖 |
|------|------|---------|---------|------|
| **User-CF** | 30% | 相似用户喜欢什么？ | 用户行为评分矩阵 | 需要历史行为 |
| **Item-CF** | 25% | 相似资源有哪些？ | 资源同现矩阵 | 需要历史行为 |
| **Content-Based** | 25% | 内容标签匹配度？ | 用户标签 + 资源关键词 | 需要兴趣标签 |
| **Profile-Based** | 20% | 心理状态需要什么？ | 测评结果 + 资源关键词 | 需要测评数据 |

### 1.2 权重设计逻辑

为什么 User-CF 权重最高（30%），心理画像最低（20%）？

- **User-CF 30%**：协同过滤的"人多力量大"效应——当用户有足够行为数据时，相似用户的行为模式是**最可靠**的推荐信号
- **Content-Based 25% / Item-CF 25%**：内容匹配和物品相似是协同过滤的有效补充，能够发现 User-CF 遗漏的长尾资源
- **Profile-Based 20%**：心理画像只有 3 个 level（low/medium/high），区分度有限；但它**永不断电**——即使用户零行为数据，也能启动推荐

### 1.3 架构特点

- **时间衰减机制**：融入所有协同过滤计算中，近期行为权重更高
- **Min-Max归一化**：将不同量纲的评分统一到 [0, 1] 区间
- **多样性过滤**：避免同类型资源过度集中，保证推荐多样性
- **冷启动处理**：新用户借助心理画像和热门资源快速启动
- **动态兴趣学习**：通过行为日志持续更新用户兴趣标签

---

## 二、数据库表架构（7张核心表）

### 2.1 表关系总览

```
user (用户基础信息)
  │
  ├── user_resource_rating (用户行为评分) ────┐
  │   └── 权重/评分/时间                  协同过滤计算
  │
  ├── user_interest_tags (用户兴趣标签) ──────┐
  │   └── 标签名/分数/来源               内容匹配计算
  │
  ├── user_psychological_profile (心理画像) ───┐
  │   └── 压力/焦虑/抑郁水平            画像推荐计算
  │
  ├── user_behavior_log (行为日志) ───────────┐
  │   └── 明细行为记录                  兴趣标签更新
  │
  └── (前端交互产生行为数据)
      
education_resource (教育资源)
  │
  ├── resource_keywords (资源关键词)
  │   └── 关键词/TF-IDF分数
  │
  └── recommendation_result (推荐结果缓存)
      └── 算法类型/置信度/推荐理由/过期时间
```

### 2.2 各表详细说明

#### 表1：user_resource_rating（用户资源评分表）

**用途**：存储**<u>用户对资源的评分数据</u>**，是协同过滤的核心数据源

**关键字段**：

| 字段 | 类型 | 说明 |
|------|------|------|
| user_id | BIGINT | 用户ID |
| resource_id | BIGINT | 资源ID |
| rating_type | TINYINT | 1-浏览 2-点赞 3-收藏 4-完成学习 5-分享 |
| score | DECIMAL(3,2) | 评分值：浏览=1, 点赞=3, 收藏=4, 完成=5, 分享=5 |
| weight | DECIMAL(3,2) | 行为权重（初始1.0，随时间衰减） |
| created_at | DATETIME | 行为发生时间 |

**数据含义**：同一用户对同一资源可有多条记录（不同行为类型），计算时通过SUM聚合

#### 表2：user_interest_tags（用户兴趣标签表）

**用途**：存储用户的兴趣标签和兴趣度分数，是内容推荐的核心数据源

**关键字段**：

| 字段 | 类型 | 说明 |
|------|------|------|
| user_id | BIGINT | 用户ID |
| tag_name | VARCHAR(50) | 标签名称（如"考试焦虑"、"压力管理"） |
| tag_category | VARCHAR(50) | 标签分类：emotion/stress/relationship/career等 |
| interest_score | DECIMAL(5,2) | 兴趣度(0-100)，越高表示越感兴趣 |
| source | VARCHAR(50) | 标签来源：assessment(测评)/behavior(行为)/diary(日记) |

#### 表3：user_psychological_profile（用户心理画像表）

**用途**：存储用户的心理测评结果，用于画像推荐和冷启动

**关键字段**：

| 字段 | 类型 | 说明 |
|------|------|------|
| user_id | BIGINT | 用户ID |
| stress_level | VARCHAR(20) | 压力水平：low/medium/high |
| anxiety_level | VARCHAR(20) | 焦虑水平：low/medium/high |
| depression_level | VARCHAR(20) | 抑郁水平：low/medium/high |
| dominant_issues | VARCHAR(200) | 主要问题（逗号分隔） |
| personality_traits | VARCHAR(200) | 性格特征 |
| coping_style | VARCHAR(50) | 应对方式：active/passive |

#### 表4：resource_keywords（资源关键词表）

**用途**：存储资源的关键词和TF-IDF分数，用于与用户兴趣标签做匹配

**关键字段**：

| 字段 | 类型 | 说明 |
|------|------|------|
| resource_id | BIGINT | 资源ID |
| keyword | VARCHAR(50) | 关键词 |
| tfidf_score | DECIMAL(5,2) | TF-IDF权重分数 |
| category | VARCHAR(50) | 关键词类别：problem/solution等 |

#### 表5：user_behavior_log（用户行为日志表）

**用途**：记录用户所有操作的明细日志，是兴趣标签动态更新的数据源

**关键字段**：

| 字段 | 类型 | 说明 |
|------|------|------|
| user_id | BIGINT | 用户ID |
| behavior_type | VARCHAR(20) | 行为类型：view/click/like/favorite/complete/share |
| target_type | VARCHAR(20) | 目标类型：resource/post/scale |
| target_id | BIGINT | 目标ID |
| duration | INT | 停留时长(秒) |
| extra_data | JSON | 额外数据（浏览的资源标签等） |

#### 表6：recommendation_result（推荐结果缓存表）

**用途**：缓存每次生成的推荐结果，支持7天过期和点击追踪

**关键字段**：

| 字段 | 类型 | 说明 |
|------|------|------|
| user_id | BIGINT | 用户ID |
| resource_id | BIGINT | 推荐资源ID |
| algorithm_type | VARCHAR(50) | 算法来源：user_cf/item_cf/content_based/profile_based/hybrid/hot |
| confidence_score | DECIMAL(5,2) | 推荐置信度(0-100) |
| reason | VARCHAR(200) | 推荐理由 |
| display_order | INT | 展示顺序 |
| is_clicked | TINYINT | 是否被用户点击 |
| expired_at | DATETIME | 过期时间（7天后） |

#### 表7：education_resource（教育资源表）

**用途**：存储被推荐的资源本身，全局共用（不属于推荐模块独有）

**推荐相关字段**：tags（标签）, view_count（浏览量）, status（状态）, type（类型）

---

## 三、推荐流程全景图

```
┌─────────────────────────────────────────────────────────────┐
│                    推荐请求入口                               │
│            GET /recommendation/personalized?topN=10          │
└────────────────────────┬────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────┐
│  阶段1：检查缓存（recommendation_result）                      │
│  - 查询未过期的推荐结果                                        │
│  - 如果有缓存且数量足够 → 直接返回                              │
│  - 否则 → 触发重新计算                                         │
└────────────────────────┬────────────────────────────────────┘
                         │ (缓存未命中或过期)
                         ▼
┌─────────────────────────────────────────────────────────────┐
│  阶段2：四路推荐算法并行计算                                    │
│                                                              │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐       │
│  │ User-CF  │ │ Item-CF  │ │ Content  │ │ Profile  │       │
│  │ (30%)    │ │ (25%)    │ │ (25%)    │ │ (20%)    │       │
│  └────┬─────┘ └────┬─────┘ └────┬─────┘ └────┬─────┘       │
│       │             │             │             │             │
│       └─────────────┴─────────────┴─────────────┘             │
│                         │                                     │
│                         ▼                                     │
│              Min-Max归一化 (0~1)                               │
│                         │                                     │
│                         ▼                                     │
│              加权融合 + 去重排序                                │
└────────────────────────┬────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────┐
│  阶段3：后处理                                                │
│  - enrichWithResourceDetails：验证资源存在且已发布             │
│  - applyDiversityFilter：多样性过滤（每类最多40%）              │
│  - 数量不足 → 心理画像补充 → 热门资源兜底                       │
└────────────────────────┬────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────┐
│  阶段4：结果持久化                                            │
│  - 删除用户旧的推荐结果                                        │
│  - 插入新推荐结果（7天过期）                                    │
│  - 返回 topN 条给前端                                         │
└─────────────────────────────────────────────────────────────┘
```

---

## 四、四种推荐算法详解

### 4.0 算法原理总览

在深入每个算法之前，先理解三种推荐范式的基本数学思想：

| 范式 | 数学本质 | 类比理解 |
|------|---------|---------|
| **协同过滤** | 矩阵分解 / 相似度计算 | "物以类聚，人以群分" |
| **内容推荐** | 向量空间模型 / 余弦匹配 | "你喜欢苹果，给你推荐梨" |
| **规则推荐** | IF-THEN 专家规则 | "你焦虑高 → 推荐放松训练" |

四种算法覆盖了这三种范式：User-CF 和 Item-CF 属于协同过滤，Content-Based 属于内容推荐，Profile-Based 属于规则推荐。

---

### 4.1 算法一：基于用户的协同过滤（User-CF） — 权重30%

#### 4.1.1 原理阐述

**核心思想**：如果两个用户对大量资源有相似的评分模式，那么他们在其他资源上的偏好也可能相似。

**数学基础**：User-CF 本质上是一个 **K近邻（K-Nearest Neighbors）** 问题：

1. **构建用户-资源评分矩阵**：每一行是一个用户，每一列是一个资源，矩阵元素是评分值
2. **计算用户相似度**：用余弦相似度衡量两个用户评分向量在高维空间中的夹角
3. **预测未知评分**：取 K 个最相似用户的评分，用相似度加权平均
4. **推荐 Top-N**：按预测评分降序排列

**为什么用余弦相似度而非欧氏距离？** 在评分矩阵中，不同的用户的评分尺度不同（有人习惯打高分，有人习惯打低分）。余弦相似度关注评分向量的 **方向**（偏好模式），而非绝对大小，因此对不同评分尺度具有鲁棒性。

**为什么需要时间衰减？** 用户的兴趣会随时间变化。一个用户 3 天前的浏览行为，比 90 天前的更值得参考。如果不做时间衰减，旧行为会"锁死"推荐方向。

#### 4.1.2 核心公式

**余弦相似度（带时间衰减）**：

\[
similarity(u, v) = \frac{\sum_{i \in I_{uv}} P_{u,i} \times P_{v,i}}{\sqrt{\sum_{i \in I_u} (P_{u,i})^2} \times \sqrt{\sum_{i \in I_v} (P_{v,i})^2}}
\]

其中有效评分 \(P_{u,i}\) 融合了行为强度和时间衰减：

\[
P_{u,i} = \sum_{b \in behaviors(u,i)} \frac{score_b \times weight_b}{1 + 0.01 \times days\_since(b)}
\]

#### SQL实现

```sql
-- 第一步：计算有效评分（融入时间衰减）
SELECT user_id, resource_id, 
  SUM(score * weight / (1 + 0.01 * DATEDIFF(NOW(), created_at))) as score
FROM user_resource_rating 
WHERE user_id = #{userId} 
GROUP BY user_id, resource_id

-- 第二步：计算用户相似度
SELECT r2.user_id, COUNT(*) as common_resources,
  SUM(r1_eff.score * r2_eff.score) / 
  (SQRT(SUM(r1_eff.score * r1_eff.score)) * SQRT(SUM(r2_eff.score * r2_eff.score))) as similarity
FROM (子查询1) r1_eff
JOIN (子查询2) r2_eff ON r1_eff.resource_id = r2_eff.resource_id
JOIN education_resource er ON r1_eff.resource_id = er.id
WHERE er.status = 1
GROUP BY r2_eff.user_id
HAVING common_resources >= 2   -- 至少2个共同评分资源
ORDER BY similarity DESC
LIMIT 10
```

#### 时间衰减效果示例

```
用户对资源1的评分：
  - 3天前浏览(1分)：effective = 1 × 1.0 / (1 + 0.03) = 0.971
  - 30天前点赞(3分)：effective = 3 × 1.0 / (1 + 0.30) = 2.308
  - 90天前完成(5分)：effective = 5 × 1.0 / (1 + 0.90) = 2.632

总有效评分 = 0.971 + 2.308 + 2.632 = 5.911

对比：无衰减总和 = 1 + 3 + 5 = 9.0
可见：近期行为贡献了更大比例的有效评分
```

#### 预测评分

```java
// 对于每个相似用户v，预测目标用户u对资源i的评分
predictedScore(u, i) = Σ(similarity(u, v) × rating(v, i))

// 例如：
// 相似用户v1: 相似度0.85, 对资源7评分4.5 → 贡献 0.85 × 4.5 = 3.825
// 相似用户v2: 相似度0.60, 对资源7评分3.0 → 贡献 0.60 × 3.0 = 1.800
// 最终预测评分 = 3.825 + 1.800 = 5.625
```

---

### 4.2 算法二：基于物品的协同过滤（Item-CF） — 权重25%

#### 4.2.1 原理阐述

**核心思想**：如果大量用户同时喜欢资源 A 和资源 B，那么 A 和 B 就是相似的。一个用户喜欢 A，就推荐 B。

**与 User-CF 的本质区别**：

| 维度 | User-CF | Item-CF |
|------|---------|---------|
| 计算对象 | 用户-用户的相似度 | 物品-物品的相似度 |
| 适用场景 | 用户数 < 物品数的场景 | 物品数 < 用户数的场景 |
| 推荐逻辑 | "和你的同类人喜欢什么" | "和你喜欢的资源相似的是什么" |
| 解释性 | 社交化（"别人也喜欢"） | 内容化（"和你喜欢的类似"） |
| 稳定性 | 用户变化快，推荐波动大 | 物品关系稳定，推荐更稳定 |

**为什么需要 Item-CF 补充 User-CF？**

- User-CF 依赖相似用户群体，新物品（冷物品）无人评分则无法推荐 → Item-CF 可以通过物品间的关系发现它
- User-CF 偏向热门（热门资源被多人评分，相似度计算更准确）→ Item-CF 更容易发现长尾物品
- 两个协同过滤方向互补：一个横向（用户间），一个纵向（物品间）

**数学基础**：Item-CF 与 User-CF 是对偶问题——将用户-资源评分矩阵转置后，Item-CF 的数学形式与 User-CF 完全一致。

#### 4.2.2 核心公式

与 User-CF 对称，计算资源之间的余弦相似度：

\[
similarity(i, j) = \frac{\sum_{u \in U_{ij}} P_{u,i} \times P_{u,j}}{\sqrt{\sum_{u \in U_i} (P_{u,i})^2} \times \sqrt{\sum_{u \in U_j} (P_{u,j})^2}}
\]

#### SQL实现

```sql
-- 同样融入了时间衰减的有效评分计算
-- 找到与资源i共同被用户评分过的资源j
SELECT r2_eff.resource_id, COUNT(*) as common_users,
  SUM(r1_eff.score * r2_eff.score) / 
  (SQRT(SUM(r1_eff.score * r1_eff.score)) * SQRT(SUM(r2_eff.score * r2_eff.score))) as similarity
FROM (子查询1：资源i的有效评分) r1_eff
JOIN (子查询2：其他资源的有效评分) r2_eff 
  ON r1_eff.user_id = r2_eff.user_id
GROUP BY r2_eff.resource_id
HAVING common_users >= 2
ORDER BY similarity DESC
LIMIT 5
```

#### 预测评分

```java
// 用户u对资源j的预测评分 = 用户对已评分资源的评分 × 资源相似度
predictedScore(u, j) = Σ(rating(u, i) × similarity(i, j))

// 例如：
// 用户喜欢资源1(评分4.0)，资源1与资源7相似度0.85 → 贡献 4.0 × 0.85 = 3.400
// 用户喜欢资源3(评分5.0)，资源3与资源7相似度0.72 → 贡献 5.0 × 0.72 = 3.600
// 最终预测评分 = 3.400 + 3.600 = 7.000
```

---

### 4.3 算法三：基于内容的推荐（Content-Based） — 权重25%

#### 4.3.1 原理阐述

**核心思想**：将用户兴趣和资源内容都表示为关键词向量，通过向量内积计算匹配度。

**数学基础**：这是一个 **向量空间模型（Vector Space Model）** 问题：

1. **用户向量**：从 `user_interest_tags` 表获取，每个标签的 `interest_score` 作为向量分量
2. **资源向量**：从 `resource_keywords` 表获取，每个关键词的 `tfidf_score` 作为向量分量
3. **匹配度**：两个向量的内积（dot product），即共有关键词的 score × tfidf 之和

**为什么用内积而非余弦相似度？** 余弦相似度只考虑方向，不考虑向量长度。但 interest_score 和 tfidf_score 都是绝对值（兴趣越强 → score 越高，关键词越重要 → tfidf 越高），用内积能同时捕捉 **匹配广度**（有多少共同关键词）和 **匹配深度**（每个关键词的强度）。

**TF-IDF 的含义**：TF-IDF（Term Frequency-Inverse Document Frequency）衡量一个词对一篇文档的重要程度：
- TF（词频）：一个词在文档中出现的频率
- IDF（逆文档频率）：该词在所有文档中的稀有程度
- TF-IDF = TF × IDF，高频但广泛出现的词（如"的"、"是"）得分低，高频且稀有的词（如"考试焦虑"）得分高

**用户兴趣标签的来源**：
| 来源 | 说明 | 示例 |
|------|------|------|
| assessment | 测评完成时自动写入 | 做 SDS 量表 → 写入"抑郁调适"(85分) |
| behavior | 浏览/学习行为自动提取 | 多次看"压力管理"文章 → 该标签分数上升 |
| diary | 情绪日记关键词分析 | 日记中出现"焦虑"高频 → 写入"焦虑应对" |

#### 4.3.2 核心公式

**标签匹配度（向量内积）**：

\[
matchScore(u, r) = \sum_{tag \in T_u \cap K_r} interest\_score(u, tag) \times tfidf\_score(r, tag)
\]

其中 \(T_u\) 是用户 u 的兴趣标签集合，\(K_r\) 是资源 r 的关键词集合。

#### SQL实现

```sql
SELECT er.id, er.title, er.type, er.cover_url, er.tags, er.difficulty,
  SUM(uit.interest_score * rk.tfidf_score) as match_score
FROM education_resource er
JOIN resource_keywords rk ON er.id = rk.resource_id
JOIN user_interest_tags uit ON rk.keyword = uit.tag_name
WHERE uit.user_id = #{userId}
  AND er.status = 1
  AND er.id NOT IN (
    SELECT resource_id FROM user_resource_rating WHERE user_id = #{userId}
  )
GROUP BY er.id
ORDER BY match_score DESC
LIMIT #{topN}
```

#### 计算示例

```
用户兴趣标签（来自测评+行为）：
  - 压力管理: 85分
  - 考试焦虑: 72分
  - 放松技巧: 65分

资源"如何应对考试焦虑"的关键词：
  - 考试焦虑: 0.85
  - 压力管理: 0.78
  - 放松技巧: 0.72

匹配度 = 85×0.78 + 72×0.85 + 65×0.72 
       = 66.3 + 61.2 + 46.8 
       = 174.3

资源"正念冥想入门指南"的关键词：
  - 正念: 0.90
  - 冥想: 0.88
  - 减压: 0.75

匹配度 = 72×0 + 85×0 + 65×0 = 0  （用户兴趣标签与资源关键词无交集）
```

---

### 4.4 算法四：基于心理画像的推荐（Profile-Based） — 权重20%

#### 4.4.1 什么是心理画像？

心理画像是本推荐系统**最具创新性**的模块。传统推荐系统（如电商、视频网站）的用户画像包含的是"年龄、性别、消费水平"等人口统计学信息，而本系统的心理画像是用户的 **心理健康状态快照**，包含五个维度：

| 维度 | 来源 | 数据示例 | 含义 |
|------|------|---------|------|
| **stress_level** | 测评综合分析 | low / medium / high | 当前压力水平 |
| **anxiety_level** | 测评综合分析 | low / medium / high | 当前焦虑水平 |
| **depression_level** | 测评综合分析 | low / medium / high | 当前抑郁水平 |
| **dominant_issues** | 量表维度最高分 | "学业压力,考试焦虑" | 最突出的心理问题 |
| **coping_style** | 测评量表分析 | active / passive | 应对方式倾向 |

#### 4.4.2 心理画像是怎么生成的？

```
用户完成心理测评（如SDS抑郁自评量表）
    │
    ▼
┌────────────────────────────────────────────┐
│  步骤1：计算量表得分                         │
│  - SDS：20题 × 1-4分 → 粗分(20-80)          │
│  - 粗分 × 1.25 → 标准分(25-100)             │
│  - 标准分 < 53 → 正常                        │
│  - 标准分 53-62 → 轻度抑郁                    │
│  - 标准分 63-72 → 中度抑郁                    │
│  - 标准分 > 72 → 重度抑郁                     │
└───────────────────┬────────────────────────┘
                    ▼
┌────────────────────────────────────────────┐
│  步骤2：映射为心理画像维度                    │
│                                              │
│  IF 重度 → stress=high, depression=high       │
│  IF 中度 → stress=medium, depression=medium   │
│  IF 轻度 → stress=medium, depression=low      │
│  IF 正常 → stress=low, depression=low         │
│                                              │
│  不同量表侧重不同维度：                        │
│  - SDS → depression（抑郁维度）               │
│  - SCL-90 → 综合（stress/anxiety一起更新）     │
│  - UPI → 适应问题筛查                         │
└───────────────────┬────────────────────────┘
                    ▼
┌────────────────────────────────────────────┐
│  步骤3：提取 dominant_issues                  │
│                                              │
│  从量表维度得分中找出最高维度：                │
│  - 躯体化得分占比最高 → "躯体化障碍"           │
│  - 焦虑维度最高 → "焦虑"                      │
│  - 抑郁维度最高 → "抑郁"                      │
└───────────────────┬────────────────────────┘
                    ▼
┌────────────────────────────────────────────┐
│  步骤4：写入数据库                            │
│  - user_psychological_profile（INSERT/UPDATE）│
│  - user_interest_tags（同时写入兴趣标签）      │
│    如 SDS 测评 → 自动打标签"抑郁调适"(85分)    │
└────────────────────────────────────────────┘
```

#### 4.4.3 推荐是如何匹配的？（核心算法）

基于心理画像的推荐本质上是一个 **规则匹配引擎**：用户的每个心理维度 + 主要问题，与资源的每个关键词做**多条件评分**，最终按得分降序排列。

**五层评分规则**：

```
资源关键词 匹配 用户心理画像
    │
    ├─ 规则1：关键词 = dominant_issues 精确匹配 → 得分 80
    │   例：dominant_issues="考试焦虑" 匹配 keyword="考试焦虑"
    │
    ├─ 规则2：压力/焦虑高 → 推荐放松减压类 → 得分 70
    │   条件：keyword ∈ {压力管理,焦虑缓解,情绪调节,放松技巧,正念冥想}
    │          AND (stress_level ∈ {medium,high} OR anxiety_level ∈ {medium,high})
    │
    ├─ 规则3：抑郁高 → 推荐情绪调节类 → 得分 70
    │   条件：keyword ∈ {抑郁干预,情绪调节,积极心理学,自我关怀}
    │          AND depression_level ∈ {medium,high}
    │
    ├─ 规则4：特定问题匹配（高置信度）→ 得分 75
    │   条件：dominant_issues LIKE '%就业%' AND keyword ∈ {职业规划,就业指导,职业发展}
    │   条件：dominant_issues LIKE '%学业%' AND keyword ∈ {时间管理,学习效率,考试焦虑}
    │
    └─ 规则5：兜底匹配 → 得分 50
        条件：以上都不满足，但资源关键词在心理健康领域通用词汇
```

**设计逻辑**：
- **精确匹配得分最高（80分）**：如果 dominant_issues 直接等于资源关键词，说明"对症下药"
- **特定问题次高（75分）**：学业/就业问题是大学生最常见困扰，主观报告的准确度高
- **水平触发（70分）**：基于量表分数的水平判断有专业依据，但比精确匹配笼统
- **兜底（50分）**：保证有一定推荐量，但分数最低，不抢占前几个规则的推荐位

#### 4.4.4 为什么这套规则有临床心理学依据？

**1. 压力-焦虑-抑郁三元模型**：在心理健康领域，压力、焦虑、抑郁是三个高度相关但独立可测的维度。当用户在某个维度上得分为 medium/high 时，临床共识是推荐该维度的干预资源。

**2. 问题导向干预（Problem-Oriented Intervention）**：当用户明确指出问题（如"考试焦虑"）时，直接推荐该问题的针对性资源效果最好——这是循证心理学的核心理念。

**3. 阶梯式干预（Stepped Care）**：
- low 水平 → 推荐通用心理健康教育资源（学习型）
- medium 水平 → 推荐针对性自助资源（干预型）
- high 水平 → 推荐专业求助引导（转介型）

**4. 大学生特有心理问题**：就业焦虑、学业压力、人际关系是大学生群体的三大核心困扰，规则中针对这三个方向做了专项匹配。

#### 4.4.5 与其他算法的关系：冷启动引擎 + 兜底补充

心理画像推荐在整个系统中扮演两个关键角色：

**角色一：冷启动引擎**
```
新用户注册 → 做心理测评 → 生成心理画像 → Profile-Based 推荐
                                          ↓
                                    用户获得第一批推荐
                                          ↓
                              开始浏览/点赞/学习资源
                                          ↓
                              产生行为数据 → User-CF/Item-CF 开始工作
```

**角色二：兜底补充**

在正常推荐流程中，当其他三个算法推荐数量不足时自动补充：
```java
if (推荐数量 < topN) {
    用 Profile-Based 补足到 topN
}
if (仍然不足) {
    用热门资源兜底
}
```
这就是 Profile-Based 虽权重仅 20% 但不能被删除的原因——它是整条推荐链路的"安全气囊"。

#### 4.4.6 数据流完整示例

以用户张同学完成 SDS 量表为例：

```
1. 测评完成
   ├─ 标准分: 58 → "轻度抑郁"
   └─ 维度分析: 躯体化60%, 抑郁50%, 焦虑30%
   
2. 写入 user_psychological_profile
   ├─ stress_level: "medium"
   ├─ anxiety_level: "low"  
   ├─ depression_level: "low"
   └─ dominant_issues: "躯体化障碍"
   
3. 写入 user_interest_tags
   ├─ "抑郁调适": 85分 (来源: assessment)
   ├─ "情绪管理": 80分 (来源: assessment)
   ├─ "健康管理": 75分 (来源: assessment)
   └─ "自我关怀": 78分 (来源: assessment)
   
4. Profile-Based 推荐计算
   ├─ keyword="躯体化障碍" → 精确匹配 dominant_issues → 得分80 ✓
   ├─ keyword="压力管理" → 触发规则2 (stress=medium) → 得分70 ✓
   ├─ keyword="抑郁干预" → 不触发规则3 (depression=low) → 得分50
   └─ keyword="职业规划" → 不触发 (无"就业") → 得分50
   
5. 排序结果
   └─ 躯体化相关(80分) > 压力管理(70分) > 通用资源(50分)
```

#### SQL实现

```sql
SELECT DISTINCT er.id, er.title, er.type, er.cover_url, er.tags,
  (CASE
    -- 主要问题精确匹配：得分最高
    WHEN upp.dominant_issues LIKE CONCAT('%', rk.keyword, '%') THEN 80
    -- 压力/焦虑高 → 推荐放松减压类资源
    WHEN rk.keyword IN ('压力管理','焦虑缓解','情绪调节','放松技巧','正念冥想')
      AND (upp.stress_level IN ('medium','high') OR upp.anxiety_level IN ('medium','high')) THEN 70
    -- 抑郁水平高 → 推荐情绪调节类资源
    WHEN rk.keyword IN ('抑郁干预','情绪调节','积极心理学','自我关怀')
      AND upp.depression_level IN ('medium','high') THEN 70
    -- 就业问题 → 推荐职业规划类资源
    WHEN rk.keyword IN ('职业规划','就业指导','职业发展')
      AND upp.dominant_issues LIKE '%就业%' THEN 75
    -- 学业问题 → 推荐学习相关资源
    WHEN rk.keyword IN ('时间管理','学习效率','考试焦虑')
      AND upp.dominant_issues LIKE '%学业%' THEN 75
    ELSE 50
  END) as match_score
FROM user_psychological_profile upp
CROSS JOIN resource_keywords rk
JOIN education_resource er ON rk.resource_id = er.id
WHERE upp.user_id = #{userId}
  AND er.status = 1
  AND er.id NOT IN (
    SELECT resource_id FROM user_resource_rating WHERE user_id = #{userId}
  )
HAVING match_score >= 50
ORDER BY match_score DESC
LIMIT #{topN}
```

#### 规则映射表

| 触发条件 | 匹配关键词 | 得分 | 推荐目的 |
|---------|---------|-------------|
| 触发条件 | 匹配关键词 | 得分 | 推荐目的 |
|---------|-----------|------|---------|
| dominant_issues 包含关键词 | 精确匹配 | 80 | 最精准的"对症下药" |
| dominant_issues 含"就业" | 职业规划, 就业指导, 职业发展 | 75 | 应对就业焦虑 |
| dominant_issues 含"学业" | 时间管理, 学习效率, 考试焦虑 | 75 | 应对学业压力 |
| dominant_issues 含"人际" | 人际交往, 沟通技巧, 社交恐惧 | 70 | 改善人际关系 |
| dominant_issues 含"适应" | 大学适应, 新生适应, 环境适应 | 70 | 提升适应能力 |
| stress/anxiety_level=medium/high | 压力管理, 焦虑缓解, 情绪调节, 放松技巧, 正念冥想 | 70 | 缓解压力与焦虑 |
| depression_level=medium/high | 抑郁干预, 情绪调节, 积极心理学, 自我关怀 | 70 | 改善抑郁情绪 |
| 以上都不满足 | 心理健康通用关键词 | 50 | 基础兜底推荐 |

**SQL 关键点解析**：
- CROSS JOIN resource_keywords：将用户画像与所有资源关键词做笛卡尔积，逐条打分
- HAVING match_score >= 50：用 HAVING 而非 WHERE，因为 match_score 是 CASE 计算列
- 排除已评分资源：避免推荐用户已经看过的内容

#### 4.4.7 Profile-Based 的局限性

- 规则粒度粗：只有 3 个 level，无法区分细分程度，改进方向：改为连续值 0-100 匹配
- 规则需人工维护：关键词映射需要心理学专业知识，改进方向：引入知识图谱自动关联
- 权重固定低：新用户只有 Profile 可用但权重仅 20%，改进方向：动态权重（无行为时 Profile 升至 60%）
- 无自学习能力：用户反馈不能反向优化规则，改进方向：引入强化学习或贝叶斯更新

---

### 4.5 Min-Max归一化

在融合之前，对每种算法的评分进行归一化处理：

```java
// Min-Max归一化：将分数映射到 [0, 1] 区间
double maxScore = scores.max();
double minScore = scores.min();
double range = maxScore - minScore;

if (range == 0) {
    normalized = 0.5;  // 所有分数相同时均匀分布
} else {
    normalized = (score - minScore) / range;
}
```

**为什么用 Min-Max 而非 Max-Only：**

| 方法 | 公式 | 问题 |
|------|------|------|
| Max-Only | `score / maxScore` | 最低分被压缩但不会归零，所有分数偏高 |
| Min-Max | `(score - minScore) / (maxScore - minScore)` | 充分利用[0,1]区间，区分度更好 |

---

## 五、四路混合推荐融合策略

### 5.1 融合算法

```java
// 四路加权融合
finalScore = userCfScore × 0.30   // 协同过滤（用户）
           + itemCfScore × 0.25   // 协同过滤（物品）
           + contentScore × 0.25  // 内容匹配
           + profileScore × 0.20  // 心理画像
```

### 5.2 融合流程

```
1. 四种算法各自返回 topN × 3 个候选结果（扩大候选池）
       │
2. 分别对四路结果做 Min-Max 归一化
       │
3. 以 resourceId 为 key 合并去重：
   - 同一资源被多个算法推荐 → 累加加权分数
   - 同一资源被多个算法推荐 → 合并推荐理由（去重）
       │
4. 按最终得分降序排序
       │
5. 取 topN 条
```

### 5.3 推荐理由合并示例

```
资源7被三个算法推荐：

User-CF推荐理由："与您兴趣相似的用户也喜欢"
Item-CF推荐理由："与您喜欢的资源相似"
Content推荐理由："符合您的兴趣标签"

最终显示：💡 与您兴趣相似的用户也喜欢 / 与您喜欢的资源相似 / 符合您的兴趣标签
```

### 5.4 多样性过滤

为避免推荐结果过于集中在某一类资源，融合后应用多样性过滤：

```java
// 每种类型最多占 40%
int maxSameType = (int) Math.ceil(totalSize * 0.4);

// 按序保留，超出限制的同类型资源移到溢出列表
// 如果过滤后数量太少（<3），从溢出列表补充
```

---

## 六、兴趣标签动态更新机制

### 6.1 触发方式

| 触发方式 | 接口 | 说明 |
|---------|------|------|
| 用户手动 | POST `/recommendation/refresh-tags` | 前端刷新按钮 |
| 系统定时 | 定时任务（建议每日凌晨） | 批量更新所有活跃用户 |

### 6.2 更新流程

```
1. 查询用户最近30天的行为数据（user_resource_rating）
       │
2. 按资源关键词聚合 + 时间衰减权重计算
       │
   SQL: 详见 UserResourceRatingMapper.getUserBehaviorKeywordStats()
       │
3. 将聚合分数归一化到 0-100
       │
4. 逐个写入 user_interest_tags（UPSERT，保留最大分数）
       │
5. 清理超过60天未更新的 source='behavior' 标签
```

### 6.3 聚合SQL

```sql
-- 从用户行为中提取关键词兴趣度
SELECT rk.keyword, rk.category,
  SUM(urr.score * urr.weight / (1 + 0.01 * DATEDIFF(NOW(), urr.created_at)) 
      * rk.tfidf_score) as aggregated_score,
  COUNT(DISTINCT urr.resource_id) as resource_count
FROM user_resource_rating urr
JOIN resource_keywords rk ON urr.resource_id = rk.resource_id
WHERE urr.user_id = #{userId}
  AND urr.created_at >= DATE_SUB(NOW(), INTERVAL 30 DAY)
GROUP BY rk.keyword, rk.category
ORDER BY aggregated_score DESC
LIMIT 20
```

---

## 七、冷启动处理策略

### 7.1 冷启动场景识别

```
用户请求推荐
    │
    ├── 有 user_resource_rating 记录？
    │   ├── YES → 正常推荐流程
    │   └── NO  → 冷启动场景
    │
    ├── 有 user_psychological_profile？
    │   ├── YES → 使用心理画像推荐 (profile-based)
    │   └── NO  → 使用热门资源推荐
    │
    └── 数量不足？→ 按优先级逐级补充
```

### 7.2 补充优先级

| 优先级 | 策略 | 方法 | 触发条件 |
|--------|------|------|---------|
| 1 | 心理画像推荐 | `getProfileBasedRecommendations()` | 数量不足且用户有测评记录 |
| 2 | 热门资源推荐 | `getHotResources()` | 心理画像推荐后仍不足 |
| 3 | 默认分数 0.3 | 作为兜底 | 最终保障 |

---

## 八、前端展示

### 8.1 API接口

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/recommendation/personalized` | 四路混合推荐（默认） |
| GET | `/recommendation/user-cf` | 协同过滤推荐 |
| GET | `/recommendation/item-cf` | 物品协同过滤推荐 |
| GET | `/recommendation/content-based` | 基于内容推荐 |
| GET | `/recommendation/profile-based` | 基于心理画像推荐 |
| POST | `/recommendation/click` | 记录推荐点击 |
| POST | `/recommendation/refresh-tags` | 刷新兴趣标签 |

### 8.2 返回数据结构

```json
[
  {
    "resourceId": 7,
    "title": "如何应对考试焦虑",
    "type": 1,
    "coverUrl": null,
    "tags": "考试焦虑,压力管理,放松技巧",
    "difficulty": 1,
    "finalScore": 0.7745,
    "algorithm": "hybrid",
    "reasons": [
      "与您兴趣相似的用户也喜欢",
      "与您喜欢的资源相似",
      "符合您的兴趣标签"
    ]
  }
]
```

### 8.3 前端展示效果

- **混合推荐Tab**：默认视图，展示四路融合后的最优推荐
- **协同过滤Tab**：仅展示User-CF的结果
- **兴趣匹配Tab**：仅展示Content-Based的结果
- **心理画像Tab**：仅展示Profile-Based的结果
- **刷新按钮**：手动触发兴趣标签更新

---

## 九、推荐效果评估

### 9.1 点击率统计

```sql
-- 最近7天的推荐点击率
SELECT 
  COUNT(*) as total_recommendations,
  SUM(is_clicked) as total_clicks,
  ROUND(SUM(is_clicked) / COUNT(*) * 100, 2) as click_rate
FROM recommendation_result
WHERE user_id = #{userId}
  AND created_at >= DATE_SUB(NOW(), INTERVAL 7 DAY);
```

### 9.2 算法效果对比

```sql
-- 各算法的点击率对比
SELECT 
  algorithm_type,
  COUNT(*) as count,
  ROUND(SUM(is_clicked) / COUNT(*) * 100, 2) as click_rate,
  ROUND(AVG(confidence_score), 2) as avg_confidence
FROM recommendation_result
WHERE user_id = #{userId}
GROUP BY algorithm_type
ORDER BY click_rate DESC;
```

### 9.3 多样性评估

```sql
-- 推荐结果的类型分布
SELECT 
  er.type,
  COUNT(*) as count,
  ROUND(COUNT(*) / (SELECT COUNT(*) FROM recommendation_result 
    WHERE user_id = #{userId}) * 100, 2) as percentage
FROM recommendation_result rr
JOIN education_resource er ON rr.resource_id = er.id
WHERE rr.user_id = #{userId}
GROUP BY er.type;
```

---

## 十、性能优化策略

### 10.1 推荐结果缓存

- `recommendation_result` 表自带7天过期机制
- 每次请求先检查是否有未过期的缓存
- 有缓存且数量足够 → 直接返回，跳过计算

### 10.2 批量预计算（可选）

```sql
-- 定时任务：凌晨为所有活跃用户重算推荐
-- 可使用 Spring @Scheduled 注解实现
@Scheduled(cron = "0 0 2 * * ?")  // 每天凌晨2点
public void batchRefreshRecommendations() {
    List<Long> activeUserIds = userMapper.selectActiveUserIds();
    for (Long userId : activeUserIds) {
        getPersonalizedRecommendations(userId, 15);
    }
}
```

### 10.3 SQL优化

- 所有协同过滤查询使用子查询预聚合有效评分
- `user_resource_rating` 表建有 `(user_id)`, `(resource_id)` 索引
- `recommendation_result` 表建有 `(user_id, display_order)`, `(expired_at)` 索引
- `user_behavior_log` 表建有 `(user_id, created_at)`, `(user_id, behavior_type, created_at)` 复合索引

---

## 十一、与旧版本的关键改进对比

| 维度 | v1.0（旧版） | v2.1（新版） |
|------|-------------|-------------|
| 算法数量 | 三路（User-CF + Item-CF + Content） | **四路**（+ Profile-Based） |
| 权重配置 | 0.35/0.35/0.30 | 0.30/0.25/0.25/0.20 |
| 时间衰减 | ✗ 未实现 | ✓ 融入所有CF计算 |
| 归一化 | Max-Only | **Min-Max** |
| 兴趣标签更新 | TODO（空实现） | ✓ 基于行为日志自动提取 |
| 多样化 | ✗ | ✓ 类型比例限制 40% |
| 冷启动 | 仅热门资源 | **心理画像 → 热门资源 两级兜底** |
| 行为日志 | 定义但未使用 | ✓ 驱动兴趣标签更新 |
| 冗余表 | user_profile + recommendation_cache | **已清理，每张表都有明确用途** |
| API数量 | 5个 | **7个**（+ profile-based + refresh-tags） |
| 权重配置 | 硬编码 | 硬编码（可配置化待实现） |

---

## 十二、数据变更与算法联动全景图

前面四至十一章讲解了"数据是什么"和"算法怎么算"。本章回答第三个关键问题：**数据什么时候变、谁让它变、变了之后影响哪个算法**。

### 12.1 数据-算法因果链总览

```
┌─────────────────────────────────────────────────────────────────┐
│                      用户操作层（前端触发）                        │
├────────────┬───────────┬─────────────┬───────────┬──────────────┤
│  做心理测评  │ 浏览资源   │ 点赞/收藏   │ 完成学习   │  分享资源    │
│            │           │             │           │              │
└─────┬──────┴─────┬─────┴──────┬──────┴─────┬─────┴──────┬───────┘
      │            │            │            │            │
      ▼            └────────────┴──────┬─────┘            │
┌──────────┐                          │                  │
│ 测评模块  │                          ▼                  │
│ (service)│              ┌─────────────────────┐         │
└────┬─────┘              │  resource模块        │         │
     │                    │ (recordStudy等)      │◄────────┘
     │                    └──────────┬──────────┘
     │                               │
     ▼                               ▼
┌─────────────────────────────────────────────────────────────┐
│                    数据写入层（表变更）                        │
├──────────────┬──────────────┬──────────────┬────────────────┤
│psychological │interests_tags│resource_rating│behavior_log   │
│   _profile   │              │              │                │
└──────┬───────┴──────┬───────┴──────┬───────┴───────┬────────┘
       │              │              │               │
       ▼              ▼              ▼               ▼
┌─────────────────────────────────────────────────────────────┐
│                  算法影响层（哪些算法受影响）                   │
├──────────────┬──────────────┬──────────────┬────────────────┤
│ Profile-Based│Content-Based │ User-CF      │ tags(间接)     │
│              │              │ Item-CF      │ Content-Based  │
└──────────────┴──────────────┴──────────────┴────────────────┘
```

### 12.2 六条核心数据链

每条链完整描述：**用户操作 → 接口调用 → 表变更 → 算法影响 → 下次推荐变化**

---

#### 链1：做心理测评 → Profile-Based + Content-Based

```
① 用户操作
   前端 /pages/assessment/test.vue → 完成 SDS / SCL-90 / UPI 量表

② 后端接口
   POST /assessment/submit → AssessmentServiceImpl.submitAssessment()

③ 数据写入（同一次请求中完成）
   ┌──────────────────────────────────────────────────────┐
   │ user_assessment          INSERT  测评记录              │
   │ user_psychological_profile  INSERT/UPDATE             │
   │   stress_level   = 根据等级映射 (low/medium/high)      │
   │   anxiety_level  = 根据等级映射                        │
   │   depression_level = 根据量表类型判定                   │
   │   dominant_issues = 维度最高分名称                      │
   │                                                      │
   │ user_interest_tags      UPSERT (保留最大分数)          │
   │   - SDS → "抑郁调适"(85), "情绪管理"(80), ...          │
   │   - SCL-90 → "压力管理"(80), "心理健康"(85), ...       │
   │   source = 'assessment'                               │
   └──────────────────────────────────────────────────────┘

④ 算法影响
   下次用户请求 GET /recommendation/personalized 时：
   
   Profile-Based：
     dominant_issues 更新 → 规则1精确匹配的资源集合变化
     stress/anxiety/depression_level 更新 → 规则2/3触发的资源集合变化
     
   Content-Based：
     user_interest_tags 新增测评来源标签 → 标签向量变化
     → 与 resource_keywords 的内积计算结果变化 → 匹配度排序变化

⑤ 实战案例
   用户做 SDS 前：psychological_profile 为空 → Profile-Based 返回空
   用户做 SDS 后：depression=medium, dominant_issues="躯体化障碍"
   → Profile-Based 立即能推荐 躯体化相关(80分) 和 压力管理(70分) 资源
```

---

#### 链2：浏览/点赞/收藏/完成/分享 → User-CF + Item-CF

```
① 用户操作
   前端触发以下任一行为时调用 recordStudy() 接口：
   - 打开资源详情页 (detail.vue onLoad) → 浏览
   - 点击 ❤️ (detail.vue handleToggleLike) → 点赞/取消点赞
   - 点击 ☆ 收藏 (detail.vue handleToggleFavorite) → 收藏/取消收藏
   - 点击"标记完成" (detail.vue markCompleted) → 完成学习
   - 分享资源 → 分享

② 后端接口
   POST /resource/study → recordStudy(userId, resourceId, type)
   POST /resource/like → toggleLike(userId, resourceId)
   POST /resource/favorite → toggleFavorite(userId, resourceId)

③ 数据写入
   ┌──────────────────────────────────────────────────────┐
   │ user_resource_rating    INSERT                       │
   │   user_id      = 当前用户                             │
   │   resource_id  = 被操作的资源                          │
   │   rating_type  = 1-浏览/2-点赞/3-收藏/4-完成/5-分享    │
   │   score        = 浏览=1, 点赞=3, 收藏=4, 完成=5, 分享=5│
   │   weight       = 1.0 (初始值，随时间衰减)               │
   │   created_at   = NOW()                               │
   │                                                      │
   │ user_behavior_log      INSERT  (同步记录行为明细)       │
   │   behavior_type, target_type, target_id, duration     │
   └──────────────────────────────────────────────────────┘

④ 算法影响
   下次用户请求 GET /recommendation/personalized 时：
   
   User-CF：
     用户的评分向量变化 → 与 其他用户 的余弦相似度变化
     → findSimilarUsers() 的相似用户列表可能变化（新增/排序调整）
     → 相似用户喜欢的资源集合变化 → 预测评分变化
     
   Item-CF：
     用户新增了对资源A的评分 → 以资源A为起点 findSimilarResources(A)
     → 相似资源集合进入候选池 → 预测评分 = rating(A) × similarity(A,B)
     
   时间衰减（持续影响）：
     随着时间推移，旧评分的 weight 自动降低
     → 即使没有新行为，同一个 User-CF/Item-CF 查询的结果也会随时间变化

⑤ 实战案例
   用户浏览"时间管理"文章前：rating 表无记录，User-CF 和 Item-CF 返回空
   浏览 + 点赞 + 完成后：rating 表有 3 条记录，综合评分 = 1+3+5 = 9
   → User-CF：找到同样喜欢该文章的用户 → 推荐他们喜欢的其他资源
   → Item-CF：找到与"时间管理"相似的文章 → 推荐给用户
```

---

#### 链3：持续行为 → 兴趣标签更新 → Content-Based

```
① 触发方式（两种）
   - 用户手动：前端推荐页点击"刷新"按钮 → POST /recommendation/refresh-tags
   - 系统定时：建议每日凌晨 @Scheduled 定时任务

② 后端逻辑
   RecommendationServiceImpl.updateUserInterestTags(userId)

③ 数据流转
   ┌──────────────────────────────────────────────────────┐
   │ Step1：查询 user_resource_rating + resource_keywords   │
   │   SQL：getUserBehaviorKeywordStats(userId, 30天, 20)  │
   │   从最近30天的评分记录中，按资源关键词聚合              │
   │   公式：SUM(score × weight × time_decay × tfidf)      │
   │                                                      │
   │ Step2：归一化到 0-100                                  │
   │   normalized = (rawScore / maxScore) × 100            │
   │                                                      │
   │ Step3：写入 user_interest_tags (UPSERT)                │
   │   ON DUPLICATE KEY → GREATEST(旧分, 新分)              │
   │   source = 'behavior'                                 │
   │                                                      │
   │ Step4：清理过期标签                                     │
   │   DELETE FROM user_interest_tags                      │
   │   WHERE source='behavior' AND 60天未更新               │
   └──────────────────────────────────────────────────────┘

④ 算法影响
   Content-Based：
     user_interest_tags 的 behavior 来源标签更新
     → 标签向量变化 → 与 resource_keywords 的匹配度变化
     → 推荐排序调整
     
   同时清理60天未更新的旧标签 → 过时兴趣不再影响推荐

⑤ 实战案例
   用户最近频繁浏览"就业指导"类资源：
   → refresh-tags 后提取到 "职业规划"(75分), "就业指导"(70分)
   → Content-Based 开始推荐就业相关资源
   
   用户1个月没看任何"考试焦虑"内容：
   → behavior 来源的"考试焦虑"标签分数逐渐降低（时间衰减+归一化）
   → Content-Based 中考试焦虑资源的匹配度下降
```

---

#### 链4：资源增删改 → 全部四路算法（候选池变化）

```
① 操作
   管理员在后台新增/编辑/下架资源

② 数据写入
   ┌──────────────────────────────────────────────────────┐
   │ education_resource      INSERT/UPDATE                 │
   │   status = 1（已发布）或 0（下架）                       │
   │   type, tags, difficulty, view_count ...              │
   │                                                      │
   │ resource_keywords       INSERT（新资源）/ DELETE（旧）   │
   │   keyword, tfidf_score, category                      │
   └──────────────────────────────────────────────────────┘

③ 算法影响
   全部四路算法的 SQL 查询中都有：
     JOIN education_resource er ON ... WHERE er.status = 1
   
   新增资源：候选池扩大 → 推荐结果可能包含新资源
   下架资源 (status=0)：自动从所有推荐结果中排除
   修改关键词：Content-Based 和 Profile-Based 的匹配分数变化

④ 特别注意
   - 下架资源不会立即从已缓存的 recommendation_result 中消失
   - 但 enrichWithResourceDetails() 会过滤 status != 1 的记录
   - 所以下架的资源在推荐列表里会显示为空（被过滤），7天后缓存过期彻底消失
```

---

#### 链5：推荐缓存 → 所有算法（短路效应）

```
① 何时写入缓存
   每次 getPersonalizedRecommendations() 计算完成后
   → saveRecommendationResults() 写入 recommendation_result 表

② 缓存结构
   recommendation_result：
     user_id, resource_id, algorithm_type, confidence_score,
     reason, display_order, is_clicked, expired_at（7天）

③ 缓存对算法的"短路"效应
   ┌──────────────────────────────────────────────────────┐
   │ 用户请求 GET /recommendation/personalized              │
   │   │                                                  │
   │   ├─ 有缓存 → 直接返回（跳过所有算法计算）               │
   │   │   注意：缓存期间，即使用户行为数据变了，              │
   │   │   推荐结果也不变化！直到缓存过期                     │
   │   │                                                  │
   │   └─ 无缓存/过期 → 触发四路算法重算 → 写入新缓存         │
   └──────────────────────────────────────────────────────┘

④ 关键启示
   - 用户做完测评后不会立即看到推荐变化（除非缓存刚好过期）
   - 缓存过期时间 = created_at + 7天
   - 如果需要立即生效，需手动清空该用户的 recommendation_result 记录
```

---

#### 链6：点击反馈 → 效果评估（不直接影响算法）

```
① 用户操作
   点击推荐列表中的某个资源 → POST /recommendation/click

② 数据写入
   recommendation_result.is_clicked = 1

③ 影响
   当前版本：仅用于统计点击率，不反向优化算法
   未来方向：可用作 CTR 评估指标，驱动权重动态调整
```

---

### 12.3 数据-算法联动矩阵

| 用户操作 | 写入的表 | 变化的字段 | 影响算法 | 生效时机 |
|---------|---------|-----------|---------|---------|
| 做心理测评 | user_psychological_profile | stress/anxiety/depression_level, dominant_issues | Profile-Based | 下次推荐请求（无缓存时） |
| 做心理测评 | user_interest_tags | 新增 assessment 来源标签 | Content-Based | 下次推荐请求（无缓存时） |
| 做心理测评 | user_assessment | 测评记录 | 无直接影响 | - |
| 浏览资源 | user_resource_rating | rating_type=1, score=1 | User-CF, Item-CF | 下次推荐请求（无缓存时） |
| 浏览资源 | user_behavior_log | behavior_type='view' | 兴趣标签（间接） | 下次 refresh-tags 时 |
| 点赞资源 | user_resource_rating | rating_type=2, score=3 | User-CF, Item-CF | 下次推荐请求（无缓存时） |
| 收藏资源 | user_resource_rating | rating_type=3, score=4 | User-CF, Item-CF | 下次推荐请求（无缓存时） |
| 完成学习 | user_resource_rating | rating_type=4, score=5 | User-CF, Item-CF | 下次推荐请求（无缓存时） |
| 分享资源 | user_resource_rating | rating_type=5, score=5 | User-CF, Item-CF | 下次推荐请求（无缓存时） |
| 手动刷新标签 | user_interest_tags | behavior 来源标签更新 | Content-Based | 下次推荐请求（无缓存时） |
| 管理员新增资源 | education_resource + resource_keywords | 新资源入库 | 全部四路 | 下次推荐请求（无缓存时） |
| 管理员下架资源 | education_resource.status | 设为 0 | 全部四路（排除） | 立即（enrich 过滤） |
| 推荐生成 | recommendation_result | 新缓存写入 | 全部四路（短路） | 7天内不再重算 |
| 点击推荐 | recommendation_result | is_clicked=1 | 无（仅统计） | - |

### 12.4 时间维度上的数据-算法演变

以一个新用户从注册到使用30天的完整时间线为例：

```
Day 0（注册）
  │  无任何数据 → 所有算法返回空 → 冷启动触发
  │
Day 1（做完SDS测评）
  │  ├─ user_psychological_profile INSERT → Profile-Based 激活
  │  ├─ user_interest_tags INSERT（assessment来源）→ Content-Based 激活
  │  └─ 请求推荐 → [Profile 80% + Content 20%] 的四路融合结果
  │
Day 2-5（浏览+点赞5个资源）
  │  ├─ user_resource_rating 新增约10条记录
  │  ├─ User-CF：评分向量从空到有 → 开始找到相似用户
  │  ├─ Item-CF：喜欢资源A → 相似的B/C/D加入候选
  │  └─ 请求推荐 → [User-CF 30% + Item-CF 25% + Content 25% + Profile 20%]
  │
Day 7（缓存过期，首次重算）
  │  └─ 四路算法使用最新数据重新计算 → 推荐结果更新
  │
Day 10（手动刷新兴趣标签）
  │  ├─ user_interest_tags 的 behavior 来源标签更新
  │  └─ Content-Based 的标签向量反映近期真实兴趣
  │
Day 15（完成学习+收藏更多资源）
  │  ├─ user_resource_rating 评分积累，区分度更高
  │  ├─ User-CF：相似用户质量提升（共同评分资源≥2的更精准）
  │  └─ Item-CF：物品相似度计算更稳定
  │
Day 30（时间衰减效果显著）
  │  ├─ Day 1的浏览行为：weight ≈ 1/(1+0.29) = 0.775
  │  ├─ Day 25的浏览行为：weight ≈ 1/(1+0.05) = 0.952
  │  └─ 近期行为主导推荐方向，旧兴趣自然淡化
```

### 12.5 常见场景问答（答辩参考）

**Q1：用户做完测评后，推荐结果为什么没有立即变化？**
> 因为 recommendation_result 缓存了7天内的推荐结果。缓存期间直接返回缓存，不重算算法。缓存过期后，新一次请求才会使用最新的 psychological_profile 和 interest_tags 重新计算。

**Q2：如果想让测评结果立即生效怎么办？**
> 两种方案：① 测评完成后主动清空该用户的 recommendation_result 表；② 在测评提交接口中调用一次 getPersonalizedRecommendations() 并强制刷新（当前未实现）。

**Q3：浏览行为对推荐的影响是立即的吗？**
> 对 User-CF 和 Item-CF 是 "下次请求生效"（受缓存限制）。对 Content-Based 是 "下次 refresh-tags 时生效"（需要手动或定时触发兴趣标签更新）。

**Q4：为什么有些用户推荐很准，有些不准？**
> 推荐质量与数据密度正相关。行为数据越丰富（rating 表记录多），协同过滤越精准。新用户只有 Profile 和 Content 可用，所以初期推荐较粗糙——这是冷启动的固有局限。

**Q5：管理员修改了某个资源的关键词，什么时候反映到推荐？**
> 资源关键词在每次推荐请求时通过 SQL JOIN 实时查询，所以修改完**立即生效**（当然也受缓存影响）。

**Q6：用户删除了一个点赞，会影响推荐吗？**
> 当前版本的 toggleLike 实现是 INSERT 新记录（不是 DELETE 旧记录），所以"取消点赞"并不会减少 user_resource_rating 的评分总和。这是一个已知的设计简化，正确的做法应该是 DELETE 对应 rating_type 的记录。

---

## 附录A：完整数据流示例

### 场景：用户张三（student001）请求推荐

#### A.1 用户画像快照

```
用户：张三 (user_id=2)
学校：合肥工业大学 计算机 大三

测评记录（user_assessment）：
  - SDS抑郁自评：轻度抑郁 (45分)

兴趣标签（user_interest_tags）：
  - 压力管理: 85 (来源: assessment)
  - 学习效率: 78 (来源: behavior)
  - 考试焦虑: 72 (来源: assessment)

心理画像（user_psychological_profile）：
  - 压力水平: medium
  - 焦虑水平: medium
  - 抑郁水平: low
  - 主要问题: 学业压力,考试焦虑

行为数据（user_resource_rating）：
  - 考试焦虑文章：浏览+点赞
  - 时间管理文章：浏览+完成学习
  - 情绪管理视频：浏览
```

#### A.2 User-CF计算结果

```
找到2个相似用户：
  用户李四(similarity=0.644)：喜欢 抑郁干预视频(5分), 积极心理学文章(4分)
  用户王五(similarity=0.521)：喜欢 职业规划指导(5分), 沟通技巧文章(3分)

候选推荐：
  抑郁干预视频：0.644×5 + 0.521×0 = 3.220
  积极心理学文章：0.644×4 + 0.521×0 = 2.576
  职业规划指导：0.644×0 + 0.521×5 = 2.605
```

#### A.3 Item-CF计算结果

```
已评分资源 → 相似资源：
  考试焦虑文章(4分) → 正念冥想(0.85), 情绪管理(0.72)
  时间管理文章(5分) → 学习效率文章(0.78), 自我关怀(0.65)

候选推荐：
  正念冥想：4×0.85 = 3.400
  情绪管理：4×0.72 = 2.880
  学习效率：5×0.78 = 3.900
```

#### A.4 Content-Based计算结果

```
兴趣标签匹配资源关键词：
  压力管理(85) → 考试焦虑(0.78), 正念减压(0.75)
  学习效率(78) → 时间管理(0.90), 学习效率(0.85)
  考试焦虑(72) → 考试焦虑(0.85), 放松技巧(0.72)

匹配度排序：
  资源"时间管理与学习效率"：78×0.90 + 78×0.85 = 136.50
  资源"如何应对考试焦虑"：85×0.78 + 72×0.85 = 127.50
  资源"正念冥想入门指南"：85×0.75 + 72×0 = 63.75
```

#### A.5 Profile-Based计算结果

```
心理画像匹配：
  主要问题"学业压力,考试焦虑"
  → 时间管理(得分75), 考试焦虑(得分75), 压力管理(得分70), 放松技巧(得分70)

候选推荐：
  时间管理与学习效率：75
  如何应对考试焦虑：75
  压力下的自我关怀：70
  正念冥想入门指南：70
```

#### A.6 融合排序结果

```
归一化 + 加权融合：

1. 时间管理与学习效率
   User-CF: 0.00  Item-CF: 0.95  Content: 1.00  Profile: 0.75
   最终 = 0×0.30 + 0.95×0.25 + 1.00×0.25 + 0.75×0.20 = 0.6375
   理由: 与您喜欢的资源相似 / 符合您的兴趣标签 / 根据测评结果推荐

2. 正念冥想入门指南
   User-CF: 0.00  Item-CF: 0.85  Content: 0.47  Profile: 0.70
   最终 = 0×0.30 + 0.85×0.25 + 0.47×0.25 + 0.70×0.20 = 0.4700
   理由: 与您喜欢的资源相似 / 根据测评结果推荐

3. 如何应对考试焦虑
   (已评分，自动排除)

4. 情绪管理ABC理论
   User-CF: 0.00  Item-CF: 0.72  Content: 0.35  Profile: 0.00
   最终 = 0×0.30 + 0.72×0.25 + 0.35×0.25 + 0×0.20 = 0.2675
   理由: 与您喜欢的资源相似
```

---

**文档版本**：v2.1  
**适用场景**：答辩讲解、技术文档、学习参考  
**维护说明**：本文档需与代码实现保持同步，算法变更后请及时更新
