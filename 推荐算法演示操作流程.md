# 推荐算法演示操作流程

**版本**：v1.0  
**最后更新**：2026-05-07  
**适用场景**：答辩现场演示、算法效果展示  
**配套SQL**：`init_recommendation.sql` + `demo_test_data.sql`（按顺序执行）

---

## 一、演示准备（一次性）

### 1.1 数据库初始化

按顺序执行以下SQL文件：

```sql
-- 步骤1：基础表结构 + 8个用户 + 12个资源 + 测评/行为/标签/关键词数据
source init_recommendation.sql

-- 步骤2：补充4个演示专用用户 + 清除推荐缓存
source demo_test_data.sql
```

执行后数据库状态：

| 用户 | 角色 | 测评 | 心理画像 | 行为数据 | 兴趣标签 | 算法可用 |
|------|------|------|---------|---------|---------|---------|
| **student001** | 三年级计算机 | SDS轻度抑郁 | ✓ | 5条 | 3个 | **四路全开** |
| **student002** | 二年级心理学 | SDS中度抑郁 | ✓ | 6条 | 3个 | **四路全开** |
| **student003** | 四年级机械 | 焦虑中度 | ✓ | 4条 | 2个 | **四路全开** |
| **student004** | 一年级工商 | SDS正常 | ✓ | 4条 | 3个 | **四路全开** |
| **student005** | 四年级电子 | 适应轻度 | ✓ | 4条 | 2个 | **四路全开** |
| **student006** | 三年级英语 | 焦虑轻度 | ✓ | 5条 | 2个 | **四路全开** |
| **student007** | 二年级数学 | 无测评 | ✗ | 4条 | 0个 | 三路（无Profile） |
| **student008** | 三年级艺术 | 无测评 | ✗ | 2条 | 0个 | 三路（无Profile） |
| **student009** | 一年级物理 | 无 | ✗ | 0条 | 0个 | **纯冷启动** |
| **student010** | 二年级医学 | SDS重度抑郁 | ✓ | 0条 | 4个(测评源) | **画像+内容** |
| **student011** | 四年级土木 | 焦虑中度 | ✓ | 9条 | 4个(测评源) | **四路全开** |
| **student012** | 三年级新闻 | 无测评 | ✗ | 7条 | 2个(行为源) | **三路（无Profile）** |

### 1.2 确认后端服务运行

```bash
# 后端接口基地址（根据实际配置调整）
BASE_URL="http://localhost:8080"
```

### 1.3 演示工具准备

- **Postman** 或 **浏览器开发者工具**（用于直接调用API）
- **MySQL客户端**（用于验证数据库数据）
- **前端App**（推荐页面，展示最终效果）

---

## 二、演示场景总览（6个场景）

| 场景 | 演示用户 | 核心看点 | 推荐算法组合 | 耗时 |
|------|---------|---------|-------------|------|
| **A** | student009 | 纯冷启动 → 热门资源兜底 | Hot兜底 | ~1min |
| **B** | student010 | 测评驱动 → 画像精准推荐 | Profile + Content | ~2min |
| **C** | student012 | 无测评有行为 → 协同过滤主导 | User-CF + Item-CF + Content | ~2min |
| **D** | student001 | 完整数据 → 四路混合最优 | 四路混合 | ~2min |
| **E** | student011 | 就业焦虑 → 画像精准命中"就业"关键词 | Profile精确匹配 | ~1.5min |
| **F** | student002 | 中度抑郁 → 协同+画像双重信号 | 四路混合 | ~1.5min |

---

## 三、各场景详细演示流程

---

### 🔴 场景A：纯冷启动用户（student009）

> **核心看点**：没有任何数据的用户，推荐系统如何给出推荐？ → **两级兜底机制**

#### Step 1：登录获取Token

```
POST {{BASE_URL}}/auth/login
Content-Type: application/json

{
  "username": "student009",
  "password": "123456"
}
```

**期望返回**：
```json
{
  "code": 200,
  "data": {
    "token": "eyJhbGciOi...",
    "userInfo": {
      "username": "student009",
      "realName": "冷启动用户",
      "grade": "大一"
    }
  }
}
```

#### Step 2：验证数据库状态（MySQL查询）

```sql
-- 确认：无测评记录
SELECT COUNT(*) FROM user_assessment 
WHERE user_id = (SELECT id FROM user WHERE username='student009');
-- 期望：0

-- 确认：无行为记录
SELECT COUNT(*) FROM user_resource_rating 
WHERE user_id = (SELECT id FROM user WHERE username='student009');
-- 期望：0

-- 确认：无心理画像
SELECT COUNT(*) FROM user_psychological_profile 
WHERE user_id = (SELECT id FROM user WHERE username='student009');
-- 期望：0

-- 确认：无兴趣标签
SELECT COUNT(*) FROM user_interest_tags 
WHERE user_id = (SELECT id FROM user WHERE username='student009');
-- 期望：0
```

#### Step 3：请求个性化推荐

```
GET {{BASE_URL}}/recommendation/personalized?topN=10
Authorization: Bearer {{token}}
```

**期望返回**：Hot 兜底推荐，`algorithm_type = "hot"`

```json
{
  "code": 200,
  "data": [
    {
      "resourceId": 6,
      "title": "恋爱心理学基础",
      "finalScore": 0.3,
      "algorithm": "hot",
      "reasons": ["热门资源推荐"]
    },
    {
      "resourceId": 4,
      "title": "抑郁情绪的识别与应对",
      "finalScore": 0.3,
      "algorithm": "hot",
      "reasons": ["热门资源推荐"]
    },
    ...
  ]
}
```

> **答辩讲解要点**：
> - "这个用户刚注册，没有任何数据——没有做测评，没有浏览任何资源"
> - "四路算法全部返回空：User-CF找不到相似用户，Content-Based没有兴趣标签，Profile-Based没有心理画像"
> - "系统触发冷启动兜底：直接按浏览量（view_count）降序返回热门资源"
> - "这就是两级兜底的第一级——心理画像不可用时，热门资源作为最后的'安全网'"

---

### 🟠 场景B：测评新用户 — 重度抑郁（student010）

> **核心看点**：有心理画像但零行为 → Profile-Based 如何"对症下药"

#### Step 1：登录获取Token

```
POST {{BASE_URL}}/auth/login
Content-Type: application/json

{
  "username": "student010",
  "password": "123456"
}
```

#### Step 2：验证数据特征

```sql
-- 心理画像：重度抑郁
SELECT stress_level, anxiety_level, depression_level, dominant_issues 
FROM user_psychological_profile 
WHERE user_id = (SELECT id FROM user WHERE username='student010');
-- 期望：stress=high, anxiety=medium, depression=high, dominant_issues='抑郁情绪,失眠,兴趣丧失'

-- 兴趣标签：全来自测评
SELECT tag_name, interest_score, source 
FROM user_interest_tags 
WHERE user_id = (SELECT id FROM user WHERE username='student010');
-- 期望：抑郁调适(85), 情绪管理(80), 认知疗法(75), 自我关怀(78) 全部 source='assessment'

-- 行为记录：0条
SELECT COUNT(*) FROM user_resource_rating 
WHERE user_id = (SELECT id FROM user WHERE username='student010');
-- 期望：0
```

#### Step 3：请求个性化推荐

```
GET {{BASE_URL}}/recommendation/personalized?topN=5
Authorization: Bearer {{token}}
```

**期望返回特征**：
- `algorithm` 应该包含 `profile_based` 和 `content_based`（混合结果）
- `reasons` 应出现"根据您的测评结果推荐"
- 推荐的资源应与抑郁相关：抑郁情绪的识别与应对、情绪管理ABC理论、积极心理学实践

> **答辩讲解要点**：
> - "这个用户做完了SDS量表，显示重度抑郁，但还没有浏览任何资源"
> - "Profile-Based 激活：depression=high 触发规则3 → 推荐抑郁干预、情绪调节、自我关怀类资源（得分70）"
> - "dominant_issues='抑郁情绪' 精确匹配 → 与关键词'抑郁'匹配得分80（最高优先级）"
> - "Content-Based 同步激活：测评自动写入兴趣标签'抑郁调适'(85分)、'情绪管理'(80分) → 匹配资源关键词"
> - "User-CF 和 Item-CF 因无行为数据返回空 → 最终是 Profile + Content 的融合"
> - "这就是冷启动的第一级——心理画像。用户不需要积累行为数据，做完测评就能获得精准推荐"

---

### 🟡 场景C：无测评有行为 — 协同过滤主导（student012）

> **核心看点**：没做测评但有浏览偏好 → 协同过滤如何"以行为说话"（对比场景B的画像驱动）

#### Step 1：登录获取Token

```
POST {{BASE_URL}}/auth/login
Content-Type: application/json

{
  "username": "student012",
  "password": "123456"
}
```

#### Step 2：验证数据特征

```sql
-- 确认：无心理画像
SELECT COUNT(*) FROM user_psychological_profile 
WHERE user_id = (SELECT id FROM user WHERE username='student012');
-- 期望：0

-- 行为数据：7条
SELECT urr.rating_type, urr.score, er.title 
FROM user_resource_rating urr
JOIN education_resource er ON urr.resource_id = er.id
WHERE urr.user_id = (SELECT id FROM user WHERE username='student012');
-- 期望：正念冥想入门指南(浏览+点赞)、恋爱心理学基础(浏览+点赞+收藏)等

-- 兴趣标签：来自行为提取
SELECT tag_name, interest_score, source 
FROM user_interest_tags 
WHERE user_id = (SELECT id FROM user WHERE username='student012');
-- 期望：正念冥想(78, behavior)、亲密关系(82, behavior)
```

#### Step 3：请求个性化推荐

```
GET {{BASE_URL}}/recommendation/personalized?topN=5
Authorization: Bearer {{token}}
```

**期望返回特征**：
- `algorithm` 应包含 `user_cf`、`item_cf`、`content_based`（无 profile_based）
- `reasons` 可能出现："与您兴趣相似的用户也喜欢"、"与您喜欢的资源相似"
- 推荐方向：冥想放松类、恋爱心理类、情绪管理类

#### Step 4：对比查看各算法单路结果

```
GET {{BASE_URL}}/recommendation/user-cf?topN=5
Authorization: Bearer {{token}}
```
→ 有结果（有行为数据可做相似用户计算）

```
GET {{BASE_URL}}/recommendation/item-cf?topN=5
Authorization: Bearer {{token}}
```
→ 有结果（用户喜欢"正念冥想"→找到相似资源）

```
GET {{BASE_URL}}/recommendation/content-based?topN=5
Authorization: Bearer {{token}}
```
→ 有结果（标签"正念冥想""亲密关系"匹配资源关键词）

```
GET {{BASE_URL}}/recommendation/profile-based?topN=5
Authorization: Bearer {{token}}
```
→ **无结果**（没有心理画像，Profile-Based返回空）

> **答辩讲解要点**：
> - "这个用户没有做心理测评，但通过浏览行为积累了大量偏好数据"
> - "与场景B刚好对称：场景B是画像驱动（Profile+Content），这里CF+Content驱动"
> - "User-CF能找到相似用户——和其他喜欢'正念冥想'和'恋爱心理学'的人行为模式相似"
> - "Item-CF能发现：正念冥想入门指南 与 积极心理学实践/压力下的自我关怀 相似 → 推荐"
> - "Profile-Based 因无画像返回空 → 三路融合（权重重新分配）"
> - "这展示了协同过滤的核心价值：即使不了解用户的心理状态，通过行为模式也能做出合理推荐"

---

### 🟢 场景D：完整数据 — 四路混合最优（student001）

> **核心看点**：数据最全的用户 → 四路算法的融合效果（全功能展示）

#### Step 1：登录获取Token

```
POST {{BASE_URL}}/auth/login
Content-Type: application/json

{
  "username": "student001",
  "password": "123456"
}
```

#### Step 2：展示完整数据

```sql
-- 测评（轻度抑郁）
SELECT total_score, result_level, interpretation 
FROM user_assessment 
WHERE user_id = (SELECT id FROM user WHERE username='student001');
-- 期望：45分, 轻度抑郁

-- 心理画像
SELECT stress_level, anxiety_level, depression_level, dominant_issues 
FROM user_psychological_profile 
WHERE user_id = (SELECT id FROM user WHERE username='student001');
-- 期望：stress=medium, anxiety=medium, depression=low, dominant_issues='学业压力,考试焦虑'

-- 兴趣标签
SELECT tag_name, interest_score, source 
FROM user_interest_tags 
WHERE user_id = (SELECT id FROM user WHERE username='student001');
-- 期望：压力管理(85), 学习效率(78), 考试焦虑(72)

-- 行为数据
SELECT er.title, urr.rating_type, urr.score 
FROM user_resource_rating urr
JOIN education_resource er ON urr.resource_id = er.id
WHERE urr.user_id = (SELECT id FROM user WHERE username='student001');
-- 期望：考试焦虑(浏览+点赞), 时间管理(浏览+完成), 情绪管理(浏览)
```

#### Step 3：请求个性化推荐

```
GET {{BASE_URL}}/recommendation/personalized?topN=10
Authorization: Bearer {{token}}
```

**期望返回特征**：
- `algorithm` 应为 `hybrid`
- `reasons` 数组可能出现多个理由：协同过滤 + 内容匹配 + 画像推荐
- 推荐排序应有明显区分度（finalScore从高到低）

#### Step 4：展示四路各自结果

```
GET {{BASE_URL}}/recommendation/user-cf?topN=5
GET {{BASE_URL}}/recommendation/item-cf?topN=5
GET {{BASE_URL}}/recommendation/content-based?topN=5
GET {{BASE_URL}}/recommendation/profile-based?topN=5
Authorization: Bearer {{token}}
```

四路都应该有返回结果，且结果不完全相同。

> **答辩讲解要点**：
> - "张三（计算机大三）是我们数据最完整的用户，四路算法全部可用"
> - "User-CF（30%权重）：找到与张三评分模式相似的用户 → 推荐他们喜欢的资源"
> - "Item-CF（25%权重）：张三喜欢'考试焦虑'文章 → 推荐与之相似的'正念冥想'、'情绪管理'"
> - "Content-Based（25%权重）：张三的兴趣标签'压力管理(85)'、'考试焦虑(72)' → 匹配资源关键词"
> - "Profile-Based（20%权重）：dominant_issues='学业压力,考试焦虑' → 精确匹配学业/考试方向资源"
> - "四路结果经过Min-Max归一化 + 加权融合 + 多样性过滤 → 最终呈现"
> - "为什么权重这样设计？User-CF最可靠放最高，Profile区分度有限放最低——但永不掉线"
> - "可以看到同一资源被多个算法命中时，finalScore更高，reasons也更丰富"

---

### 🔵 场景E：就业焦虑精准匹配（student011）

> **核心看点**：dominant_issues 精确命中 → Profile-Based 的"对症下药"能力

#### Step 1：登录获取Token

```
POST {{BASE_URL}}/auth/login
Content-Type: application/json

{
  "username": "student011",
  "password": "123456"
}
```

#### Step 2：展示精准画像

```sql
SELECT dominant_issues, stress_level, anxiety_level 
FROM user_psychological_profile 
WHERE user_id = (SELECT id FROM user WHERE username='student011');
-- 期望：dominant_issues='就业压力,未来规划,面试焦虑', stress=high, anxiety=medium
```

关键说明：dominant_issues 包含"就业"这个关键字。

#### Step 3：单独查看 Profile-Based 推荐

```
GET {{BASE_URL}}/recommendation/profile-based?topN=5
Authorization: Bearer {{token}}
```

**期望返回**：排名靠前的应该是"职业生涯规划指导"（关键词含"职业规划"，触发规则4→得分75）

#### Step 4：查看混合推荐

```
GET {{BASE_URL}}/recommendation/personalized?topN=5
Authorization: Bearer {{token}}
```

**期望返回**："职业生涯规划指导" 应排在前面，reasons 中出现"根据测评结果推荐"

> **答辩讲解要点**：
> - "土木工程大四学生，就业焦虑非常典型"
> - "dominant_issues='就业压力,未来规划,面试焦虑' ← 注意这里包含'就业'"
> - "Profile-Based 规则4：dominant_issues LIKE '%就业%' AND keyword IN ('职业规划','就业指导','职业发展') → 得分75"
> - "规则1精确匹配：如果资源关键词直接等于'就业压力' → 得80分（最高）"
> - "同时，stress=high 触发规则2（压力管理/放松技巧 → 70分）"
> - "再加上 User-CF/Item-CF/Content 的协同 → 这是一个'对症下药'的典型案例"
> - "Profile-Based 虽然权重只有20%，但在这种精准匹配场景下，却是推荐质量最高的信号"

---

### 🟣 场景F：中度抑郁混合推荐（student002）

> **核心看点**：抑郁信号来自多个方向 → 算法的"证据汇聚"效应

#### Step 1：登录获取Token

```
POST {{BASE_URL}}/auth/login
Content-Type: application/json

{
  "username": "student002",
  "password": "123456"
}
```

#### Step 2：展示多源数据

```sql
-- 测评：中度抑郁（SDS 62分）
SELECT total_score, result_level FROM user_assessment 
WHERE user_id = (SELECT id FROM user WHERE username='student002');

-- 画像：depression=medium, dominant_issues='情绪困扰,自我价值'
SELECT depression_level, dominant_issues FROM user_psychological_profile 
WHERE user_id = (SELECT id FROM user WHERE username='student002');

-- 标签：抑郁干预(90), 情绪调节(82), 积极心理学(75)
SELECT tag_name, interest_score FROM user_interest_tags 
WHERE user_id = (SELECT id FROM user WHERE username='student002');

-- 行为：抑郁识别(浏览+点赞+收藏), 情绪管理(浏览), 积极心理(浏览+点赞)
SELECT er.title, urr.rating_type FROM user_resource_rating urr
JOIN education_resource er ON urr.resource_id = er.id
WHERE urr.user_id = (SELECT id FROM user WHERE username='student002');
```

#### Step 3：请求个性化推荐

```
GET {{BASE_URL}}/recommendation/personalized?topN=5
Authorization: Bearer {{token}}
```

**期望返回特征**：
- "抑郁情绪的识别与应对"应该排在前列（四个算法都可能打高分）
- reasons 数组可能很长（多路命中）
- finalScore 应该较高（加权累积）

> **答辩讲解要点**：
> - "李四（心理学大二），中度抑郁，这是整个演示中数据共振最强的案例"
> - "四个算法从不同方向汇聚到'抑郁'这个信号："
> - "  User-CF：其他关注抑郁资源的用户 → 推荐抑郁相关资源"
> - "  Item-CF：李四喜欢'抑郁识别' → 找到相似的'情绪管理'、'积极心理学'"
> - "  Content-Based：兴趣标签'抑郁干预(90)' → 匹配资源关键词'抑郁'(tfidf=0.95)"
> - "  Profile-Based：depression=medium → 触发规则3（抑郁干预/自我关怀 → 70分）；dominant_issues='情绪困扰' → 可能匹配相关关键词"
> - "这就是推荐系统的'证据汇聚'效应——当多个算法从不同角度指向同一个方向时，推荐结果的高置信度是有依据的"

---

## 四、演示验证SQL

演示过程中随时可用以下查询验证数据状态：

```sql
-- 1. 全面用户状态一览
SELECT 
    u.username,
    u.real_name,
    u.grade,
    CASE WHEN upp.user_id IS NOT NULL THEN 'YES' ELSE 'NO' END AS has_profile,
    COUNT(DISTINCT urr.id) AS behavior_count,
    COUNT(DISTINCT uit.id) AS tag_count,
    CASE WHEN ua.id IS NOT NULL THEN 'YES' ELSE 'NO' END AS has_assessment
FROM user u
LEFT JOIN user_psychological_profile upp ON u.id = upp.user_id
LEFT JOIN user_resource_rating urr ON u.id = urr.user_id
LEFT JOIN user_interest_tags uit ON u.id = uit.user_id
LEFT JOIN user_assessment ua ON u.id = ua.user_id
WHERE u.username LIKE 'student%'
GROUP BY u.id, u.username, u.real_name, u.grade, upp.user_id, ua.id
ORDER BY u.username;

-- 2. 查看推荐缓存
SELECT rr.user_id, u.username, rr.algorithm_type, COUNT(*) as rec_count, 
       MIN(rr.expired_at) as earliest_expiry
FROM recommendation_result rr
JOIN user u ON rr.user_id = u.id
WHERE u.username LIKE 'student%'
GROUP BY rr.user_id, u.username, rr.algorithm_type
ORDER BY u.username;

-- 3. 查看算法效果（点击率）
SELECT 
    u.username,
    rr.algorithm_type,
    COUNT(*) as total,
    SUM(rr.is_clicked) as clicked,
    ROUND(SUM(rr.is_clicked)/COUNT(*)*100, 1) as click_rate
FROM recommendation_result rr
JOIN user u ON rr.user_id = u.id
WHERE u.username LIKE 'student%'
  AND rr.is_clicked IS NOT NULL
GROUP BY u.username, rr.algorithm_type
ORDER BY u.username, click_rate DESC;
```

---

## 五、演示顺序建议

推荐的答辩演示顺序（按由简到繁）：

```
开场：场景A（student009 冷启动→热门兜底）
  │   → 一句话说明：没有任何数据，系统也知道该推荐什么
  │
  ├─ 场景B（student010 重度抑郁→画像精准推荐）
  │   → 对比说明：做完测评后，系统立即"对症下药"
  │
  ├─ 场景C（student012 无测评有行为→协同过滤）
  │   → 对比说明：另一种路径，行为代替测评也能驱动推荐
  │
  ├─ 场景E（student011 就业焦虑→精准命中）
  │   → 亮点说明：Profile-Based的"就业"关键词精准匹配
  │
  ├─ 场景F（student002 中度抑郁→证据汇聚）
  │   → 亮点说明：四路算法从不同方向指向同一结论
  │
  └─ 场景D（student001 完整数据→全功能展示）
      → 总结：数据最全的用户，四路混合融合的最优效果
```

---

## 六、答辩常见问题速答

**Q1：为什么有12个用户但只演示6个？**
> 演示的6个覆盖了所有关键场景（全数据/只有画像/只有行为/冷启动/精准匹配/证据汇聚）。其余 student003-008 是相似场景的补充，如果评委要求可以随时演示。

**Q2：推荐结果会不会每次都一样？**
> 因为有 recommendation_result 缓存（7天），首次请求计算后缓存，7天内返回相同结果。演示前已执行 `DELETE FROM recommendation_result` 清空缓存，保证实时计算。

**Q3：为什么 student009（冷启动）和 student001（全数据）返回的资源不同？**
> student009 走的是 `hot` 热门兜底算法（按 view_count 排序，统一0.3分）。student001 走的是 `hybrid` 四路混合算法（按加权融合分数排序）。两个用户的算法路径完全不同。

**Q4：如果想让测评结果立即生效怎么办？**
> 正常流程中推荐有7天缓存。演示中已清空缓存，所以每次都是实时计算。生产环境中可在测评提交接口中主动失效该用户的缓存。

**Q5：Profile-Based 权重只有20%，为什么说它很重要？**
> 因为它是永不掉线的"安全气囊"。在冷启动场景（场景B）中，当其他三个算法全部空转时，Profile-Based是唯一能给出有效推荐的算法。权重虽低但不可或缺。

---

## 附录：快速切换演示用户

```
POST {{BASE_URL}}/auth/login
Content-Type: application/json

{
  "username": "student00X",    ← 替换 1-9,A-C
  "password": "123456"
}
```

所有测试用户密码统一为 `123456`。

---

**文档版本**：v1.0  
**配套SQL**：先执行 `init_recommendation.sql`，再执行 `demo_test_data.sql`  
**重要提示**：演示前务必执行 `DELETE FROM recommendation_result;` 确保实时计算
